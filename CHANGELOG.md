# 更新记录

## 0.1.0

首个版本：给日语歌歌词里的**拉丁字母**标注**片假名读音**。

### `shake` 为什么是 シャケ：罗马音层把英文词当成了日语罗马字（本次）

用户问的：`the pretender up, up, up, up, shake, shake, shake it up, it up`
里**为什么只有 the 没被矫正**。

**先回答 the**：`the`(ザ)、`up`(アップ)、`it`(イット) 都是**离线词典命中** ——
按设计，词典是人工核过的确定答案，排在在线层前面，**不会**送去问模型
（省请求也省钱）。所以"没被矫正"其实是对的，不是漏了。

**但顺手查出了真问题**：`shake` 不是词典命中，是**罗马音层**给的
（`sha-ke` 正好切得干净 → **シャケ**，鮭）。而层序是「词典 > 罗马音 > 大模型」，
罗马音一答，大模型就没机会纠正 —— 这类词**永远读错**。

于是拿 google-10000 英文词频表做了一次审计：9974 个词里 3960 个不在词典，
其中 **240 个**会被罗马音层读成日语罗马字（`open` → オペン、`again` → アガイン、
`joke` → ジョケ、`parade` → パラデ、`poison` → ポイソン …）。两头一起补：

1. **最常见的 90 来个人工钉进词典**（`tools/seed-words.js` 里单独一块，
   含用户这句里的 `shake` → シェイク）。**故意不收**两种读法都成立的那些
   （`sake`/`dare`/`dame`/`ate`/`sage`/`bare`）—— 钉死会毁掉罗马音歌词。
   词典 6077 → **6169 条**。
2. **让在线层来仲裁剩下的**：新增 `core/enwords.js`（英文常用词表，生成物，
   由 `tools/build-enwords.js` 取词频表并剔除词典已有的，约 3700 条 / 26KB）。
   罗马音答案只要落在这张表里就标成 `confident:false`；上层对"没把握"的答案
   按**最低层**算名次，于是排它后面的在线层有资格覆盖它 ——
   `sake` 会被送去问模型、拿到 セイク 就换掉，而真正的日语罗马字
   （`sekai`/`kaze`/`shinjiteru`）不受影响，一个请求都不会多发。
   用户如果把「英文音译规则」拖到在线层前面（纯离线用法），这条名次也跟着变，
   在线层照样不会被打扰。

实测（探针）：一行 `sake sekai the shake pretender` 只问了 `sake` 和 `pretender`
两个词，`sekai` 零请求、`the`/`shake` 词典命中零请求；答案回来后
`sake` → セイク（来源 `lt-src-llm`）、`shake` → シェイク（`lt-src-dict`）。

测试：`reading.test.js` +3（英文词表里的罗马音标成没把握、真罗马音不受影响、
不传词表时行为不变），`integration.test.js` +2（只问该问的词、
纯离线顺序下依然零请求），全套 **226 → 231 全绿**。

### 大模型「请求全失败」：把诊断做扎实（本次）

用户报的：大模型请求怎么全失败了。

**先说我查到了什么**（都做了实测，不是猜）：

- 用本机的 key 直接打服务端：`GET /user/balance` → 200、余额 6.67 元；
  `POST /chat/completions` 和 `/v1/chat/completions` 都 200 并正确返回
  `{"1":"クローバー"}` —— **服务端是好的**。
- 把插件自己在 jsdom 里跑起来（llm.js 全链路 + 真 key + 真 fetch + 真接口）：
  `大模型校正回来 1 个词：命中 1`，页面上 `kaleidoscope` 注成 カレイドスコープ、
  来源类名 `lt-src-llm`，账本记下 421/10 token，`LK.llm.check()` 回「已经生效 ✓」——
  **代码路径也是好的**。

所以问题在"你那一侧的配置或网络"。为了下次能一眼定位，这次把诊断补齐：

1. **key 自动清洗**（这是"全失败"里最常见的一种）：从网页上复制 key 很容易带上
   **引号**、**前后空格**，甚至把整个 `Bearer xxx` 粘进来 —— 这三种都会让请求头不合法
   → 401。现在 `LKLLM.normalizeKey()` 会剥掉它们（引号和 Bearer 叠着也能剥），
   设置面板里**当场洗完写回输入框**，落盘的也是干净值。
2. **错误信息对号入座**：补上 `402 →（余额/额度用完）`、
   `400 →（多半是模型名不对，或这家接口不认 response_format）`；
   对**没有状态码**的失败（`Failed to fetch` 这种对用户毫无信息量的话）
   补一句人话：`（网络不通或被跨域拦住：接口地址要在网易云里能直接访问，
   并且允许 https://music.163.com 这个来源）`。
3. **`LK.llm.check()` 增加「Key 体检」**：长度、形状是否像 `sk-` 开头、
   有没有被清洗过；「请求都没成功」时直接列出 401/402/429/404/400/无状态码各自意味着什么。
   文档里也加了一张对照表（docs/notes.md 的「大模型『请求全失败』怎么查」）。

测试：`tests/llm.test.js` +5（key 清洗四种脏写法、Authorization 真的干净、
402 说清是余额、无状态码失败翻译成人话、stats 里的 key 体检），
`tests/integration.test.js` +1（面板里粘脏 key 会当场洗完并落盘），全套 **220 → 226 全绿**。

### 换歌后那行不注音了：motion 计数器其实是个永久黑名单（本次）

用户报的：换歌之后 `KiLLKiSS judy.., KiLLKiSS jude.., KiLLKiSS juda..,` 这句没注音了。

**复现**（探针：同一个 `<p>` 连续换 5 次文本，每次跑一轮扫描）：第 3 次之后那一行
**再也不标**，而且更糟 —— 整行被跳过，连上一首残留的旧注音都没人去清，
DOM 里新旧注音混在一行（用户看到的"没注音"是框架重建了节点的那种形态）。

**根因**：`motionByHost`（"这个宿主的可见文本一直在变就别追"）的 `changes` 计数
**只加不减**，加到 3 就永久放弃这个元素。而网易云换歌/滚动时**复用同一批
`<li>/<p>`，只换文本** —— 于是同一行被复用超过 3 次以后就永远不注音了。

**修法**：改成**滑动窗口**（默认 3 秒内的变化才计数，窗口一过自动重试），
而且**每次扫描都要判窗口是否过期**，不能只在"文本又变了"时判 —— 否则文本抖几下
安定下来之后就再也没人来重置计数，"某一首之后那一行永远不标"的形态依旧存在。
顺带把这条跳过路径**写进轨迹**（`未注音 文本在动 N 次/3s`）：以前它不留任何痕迹，
所以"某一行没注音"根本查不出原因。真要跟"每帧重建"的插件死磕时，
还有 churn 退避机制兜着（那条是按文字记的，换歌不会继承惩罚）。

**顺带修的第二个坑（同一类问题）**：行首就是拉丁词的行（`KiLLKiSS …` 正是这种），
老版本会把它那个**原文本节点从 DOM 里摘掉**（注音取而代之）。看着更干净，但
框架（React）还攥着那个节点的引用：换歌时它执行 `node.nodeValue = 新歌词`，
说给一个脱链节点听 —— 页面上什么都不变，我们永远看不到"这行换了"，
旧注音留在新歌的行里。现在**保留那个节点**（值清空、注音插在它后面）：
框架写进来的新歌词立刻可见，下一轮就判定失效 → 还原 → 重注。
`kept` 现在两种形态都是 true；行首那种 `head` 存的是**空串**而不是 null
（判据是 `rec.head == null || node.nodeValue === rec.head`，存 null 会变成
"不检查"，反手把框架写进来的新歌词覆盖掉）。

测试：`tests/annotate.test.js` +2（行首拉丁词的行换歌后必须能重注、
快速复用只是暂时跳过且窗口过后自动恢复），并把"注音在行首"那个既有用例改成
按「第一个看得见的东西」断言（行首那个空节点是刻意留的）。全套 **218 → 220 全绿**。

### 「这句不透明度怎么这么低」+ 按来源着色的排障功能（本次）

用户报的：`KiLLKiSS judy.., KiLLKiSS jude.., KiLLKiSS juda..,` 这句的注音特别淡；
顺带要求加一个调试功能：给不同的调用方式标上不同颜色。

**淡的根因有两个，都是 bug：**

1. **失败之后没人重扫，暂定标记一直挂着**。请求还在飞的时候那一轮是暂定的
   （`lt-pending`，样式淡），失败后进入退避、`isWaiting` 变成 false ——
   但大模型那层的失败路径**没有调 `onUpdate()`**（免费接口那边一直有），
   于是注音层永远不知道"不用再等了"。实测：那行能淡**整整一个退避周期**
   （60 秒起，翻倍到 10 分钟），真机歌词一行行滚时看不出来，停在某一句上就很显眼。
   现在失败也通知重扫，那行立刻恢复成确定值。
2. **「注音不透明度」这个设置本身是失效的**。样式里那条
   `rt.lt-rt { opacity: 1 !important }`（本意是挡住别的歌词插件给注音节点打的
   opacity）把用户的设置整个压掉了 —— 于是页面上**只有**"暂定"那 45% 看得见，
   滑块拖了也没反应。现在分开处理：`ruby.lt-ruby`（底字）锁死 1，
   `rt.lt-rt`（注音）用用户的设置，暂定再乘 0.6（跟着设置走，
   不然把不透明度调到 30% 时"暂定"反而比正常还清楚）。

**新功能：按来源着色**（设置面板「外观」→「按读音来源给注音上色」，
或控制台 `LK.colorize(true)`）。每个注音按**最终给出这个读音的层**上色：

| 颜色 | 来源 | 含义 |
| --- | --- | --- |
| 绿 | 离线词典 | 人工核过，最可信 |
| 青 | 记号 / 字母名 | `D/N/A` → ディーエヌエー、`LDK` → エルディーケー |
| 蓝 | 罗马音 | 歌词本来就是罗马字 |
| 橙 | 英文规则 | **拼写猜的**（`hello` → ヘッラオ 那一类），最该怀疑 |
| 紫 | 大模型 | 模型给的 |
| 品红 | 免费接口 | Google 给的 |

实现上：读音查询从"只返回字符串"改成 `{ kana, source }`（`annotate` 两种形状都认，
老约定不受影响），注音节点一直挂着 `lt-src-*` 类，开关只决定要不要注入颜色 CSS ——
所以开关是**瞬时生效**的，不用重扫；`relabel()` 换读音时连来源类一起换
（规则 → 大模型就是这么变的）。

测试：`llm.test.js` +1（失败必须调 onUpdate）、`integration.test.js` +4
（失败后不再淡着、着色类名与颜色 CSS、大模型换色、不透明度真的生效），
全套 **213 → 218 全绿**，`check` 0 警告。

### 加了 API 用量统计（本次）

用户要求：「加个 api 用量统计」。

新增 `core/usage.js`（独立模块）与设置面板的「API 用量」区块，记**本次 / 今天 /
累计**三份账，后两份落 `localStorage['latin-katakana.usage']`（跨天时「今天」自动翻页，
累计不动）：

| 记什么 | 大模型 | 免费接口（Google） |
| --- | --- | --- |
| 请求数 / 成功 / 失败 | ✅ | ✅ |
| 词数 / 字符数 | ✅ | ✅ |
| 输入、输出 token | ✅（接口响应里的 `usage`） | —— 这两个接口不回 usage |

几个刻意的口径：

- **只统计真发出去的请求**：命中缓存不算（省下来的请求在面板里单独显示
  「缓存命中 N 次」）；`fetch` 同步抛异常（请求根本没发出去）只记一次失败、不记请求。
- **花费是估算且默认关着**：面板多了「输入/输出单价」（元/百万 token，默认 0），
  填了才显示金额 —— 各家的价目表随时在变，写死价格等于给用户一个会过期的数字。
- **脏数据一律忽略**：账本只认有限非负数，坏 JSON / 手改坏的都当没有，不影响注音。
- `core/usage.js` 是**可选模块**：没注入时插件照常工作，`LK.usage()` 返回 null。

控制台入口：`LK.usage()` 看账本、`LK.usageReset('session'|'today'|'all')` 清零。

测试：新增 `tests/usage.test.js` 9 例（两层分开记、跨天翻页、三种清零粒度、
坏数据、花费公式、没有 localStorage 的老环境），`correct.test.js` +1（用量回调），
`integration.test.js` +4（token 记账并落盘、面板显示与三个清零按钮、单价算钱、
模块缺失时的降级），全套 **199 → 213 全绿**，`check` 0 警告。

### 读音来源顺序改成用户可调（本次）

用户要求：「让用户自己可调调用顺序」。

**设置面板新增「读音来源顺序」**：五层各一行（离线词典 / 日式罗马音 / 大模型校正 /
免费接口 / 英文音译规则），每行一个 ↑ 一个 ↓，越靠上越优先，另有「恢复默认顺序」。
控制台也留了入口：`LK.layers()` 看，`LK.layers(['rule','dict',...])` 改。

**语义只有一条**：*只有排在「当前答案」前面的在线层，才有资格覆盖它*。所以

- 把**英文规则**提到在线层前面 → 那两个在线层根本不被咨询，**一个请求都不发**
  （"不想联网 / 只看拼写音译"的用法）；
- 把**大模型**提到**词典**前面 → 连词典命中的词也让模型判一遍
  （词典偶有错条目 —— `tick` / `Ave` / `Georgette` 都是用户报过的，这是逃生门）；
- 把**免费接口**提到大模型前面 → 有 key 也先信 Google。

改完顺序会**重扫**（先整篇还原、再按新顺序重注），已注音的词会换来源；
「暂定读音 + `lt-pending` 就地改写」那套底线不变 —— 高优先层还在问的时候不空着。

**实现**：`core/reading.js` 的同步层拆成 dict / romaji / rule 三块可排序
（新增 `setOrder` / `getOrder`，默认仍是 dict > romaji > rule，65 个既有单测一条没改），
异步层（llm / google）的名次在 `main.js` 的 `readForDisplay` / `isProvisional` 里判。
形态层（记号 `D/N/A`、缩写 `I'll`、字母名 `LDK`、长音符罗马字 `Tōkyō`）**不参与排序**：
它们定的是「这个词该怎么断」而不是「读音该信谁」，永远最先判。

**容错**：`layerOrder` 会去重、补齐、丢掉不认识的项 —— 旧版本存的配置没有这个键，
手改坏的（`["rule","rule","乱七八糟"]`）也照样跑，不会少一层。

测试：`tests/reading.test.js` +7（层序互换、脏配置、形态层不受影响），
`tests/integration.test.js` +5（规则提前=零请求、模型压过词典、默认顺序下词典仍然赢、
面板 ↑↓ 改顺序并落盘、坏配置容错），全套 **187 → 199 全绿**，`check` 0 警告。

### 读音准确率：加了大模型两层（本次）

规则层再怎么修也只是**拼写音译**：`hello` → ヘッラオ、`question` → クワエサション ——
那不是人唱的音。所以接了大模型，构建期一层、运行期一层：

**一、构建期：离线词典 491 → 6046 条**（新增 `tools/expand-dict-llm.js`）

- 取英文词频表前 6000，按 40 个一批问 `deepseek-chat`，**只收纯片假名**；
  实测拦下 1 条非法（`portuguese → ポルトガル語` 混进汉字）；
  总花费 8.1 万 tokens、约 4.5 分钟（几分钱）。
- 人工表（`seed-words.js` + `reading.js` 里的例外表/小表共 628 词）**优先**，
  生成物不许覆盖人工判断；产物 `tools/seed-words-llm.js`（5555 条）由
  `npm run build:dict` 合并（新增 `npm run build:dict:llm` 重新生成）。
- **质量交叉验证**：人工核过的 491 条抽 80 条再问一遍模型，**一致率 95%**；
  4 条不一致里 2 条是人工那份更差（`cigarette` タバコ、`complaint` クレーム ——
  那是意译不是音译），另 2 条是两可写法。

**二、运行期：大模型校正层**（新增 `src/core/llm.js`，21 个单测）

- 只对**规则读出来的**词生效：词典与罗马音的结果是确定的，不被覆盖也不发请求。
- 命中缓存立刻用；没命中入队，攒 30 词或 400ms 发一次，结果回来时重扫换掉 ——
  **先显示即时结果，不阻塞注音**。
- 一个词只问一次：命中和"模型也给不出"都永久落 localStorage；
  失败不写缓存、按退避冷却重试（60s 起翻倍到 10 分钟）；限流 20 请求/分钟、20s 超时。
- **只接受纯片假名**（和 Google 那层同一判据）；任何异常都只是"这一层不工作"，
  自动退回免费的 Google 接口。没填 key 时**一个请求都不发**。
- key 只存本机 localStorage，设置面板里填、可点「测试连接」；
  默认 `api.deepseek.com` + `deepseek-chat`，任何 OpenAI 兼容接口都行。
- 真接口联调：16 个词典外的词一批问完 1.5 秒，16/16 命中且全是纯片假名。
- **接口地址自动补全**：DeepSeek / OpenAI 文档里给的是 `base_url`
  （`https://api.deepseek.com` 或 `…/v1`），照抄粘进来 POST 过去就是 **404**（实测）。
  现在会按"少一段补一段"纠正成 `/chat/completions`，设置面板里也会把纠正后的值写回输入框；
  真接口复验：三种写法（主机名 / `…/v1` / 完整地址）全部 200。
  同时把报错信息从 "HTTP 404" 改成"**地址 + 该改哪儿 + 服务端原话**"，
  401/403/429/5xx 各有对应提示。
- 端到端测试（`tests/integration.test.js`）：boot → 规则先给即时读音 → 入队 →
  一批问完 → `onUpdate` 重扫 → DOM 里的注音被换成大模型的写法。

**三、顺手补的两处**

- `tests/dict.test.js`（新增 5 个用例）：词典的数据纪律 + 一条不变量 ——
  规则层对**词典里每个词**都必须吐出纯片假名。这条当场抓出规则层的调试残留
  （`@@`、`undefined`），修完才让它常驻。
- 规则层的 r 化元音「段」修正：`ur/ir/er` 配 ア段、`or` 配 オ段、`wor-` 配 ア段
  （修正前 `turn → トゥーン`、`bird → ビード`、`river → リベー`；现在
  ターン/バード/リバー，`fork → フォーク`、`form → フォーム` 也跟着修好了）。

### 预览图改成 750×350（和 jp-furigana 同尺寸）（本次）

- `src/preview.png` 换成用户自己裁的歌词区截图（《快晴》那句
  「I know this sky loves you.」+ アイ ノウ ディス スカイ ラブズ ユー + 中文翻译），
  尺寸按 [jp-furigana](https://github.com/Leleawa/jp-furigana) 的 `preview.jpg`
  对齐成 **750×350**（2.143:1），商店里两个插件并排时构图一致。
- 做法：拿 424×237 那张小图**一次**高质量双三次放大到 750 宽（→750×419），
  再把底部背景**拉伸一行**补 24 px，然后贴底裁 750×350 —— 这样上边是干净的背景，
  切掉的全是那行**淡出的上一句译文**，而下面「いずれ全て / 就算总有一天」整行保留。
  直接按 750×350 居中裁会把上下两句各切一半。
- 包体 1.31 MB → **0.30 MB**（新预览图 170 KB，比原来那张 1585×1128 的全窗口截图小 7 倍）。

### 设置面板预览换成高考听力那句；顺带补上数字词（本次）

**一、预览示例句**

设置面板「预览」区块的示例句，从「きらめく light と clover、それから Sekai へ。」
换成高考英语听力那句名句：原文 `The shirt is nine pounds fifteen pence.`
（标成 ザ シャツ イズ ナイン パウンズ フィフティーン ペンス），
下面跟一行中文翻译「衬衫的价格为九磅十五便士」—— 翻译行**刻意不注音**，
跟真机行为一致（默认歌词页的翻译层是跳过的），免得给人错的预期。

**二、`fifteen` 显示成 フィファテエン，牵出一整族数字词的缺口**

改预览时用真读音路径探了一遍示例句，发现 `fifteen` **不在词典里**，
走规则层给的是 フィファテエン。顺着查了一整族，漏的不止一个（都是**人工钉死**）：

| 词 | 规则层/罗马音层给的 | 词典里定死 |
| --- | --- | --- |
| thirteen | シアーテエン | サーティーン |
| fourteen | フォアーテエン | フォーティーン |
| fifteen | フィファテエン | フィフティーン |
| sixteen / seventeen / eighteen | シクステエン / セベンテエン / エアイテエン | シックスティーン / セブンティーン / エイティーン |
| nineteen | ニネテエン（被罗马音层抢走） | ナインティーン |
| forty / fifty | フォータイー / フィファタイー | フォーティ / フィフティ |
| sixty…ninety | シクスタイー…ニネタイー | シックスティ…ナインティ |

根因：这批词**不在那份词频表的前 6000 里**（表里只有 one/three/nine/twenty/thirty
这些），所以从来没进过生成词表。顺手把 `two` / `seven` / `eight` / `eleven` /
`second` / `hour` / `pence` / `penny` 一并定死（它们走规则虽然读对了，但没权威来源）。
词典 6056 → **6077 条**（人工 506 → 527）。

**三、README 加图 + 两处过时说明**

- 加上全屏歌词页的真机截图（`docs/images/lyrics-page.png`，不进插件包）；
- 「在线那层还没回来时**先不标**」是上一版的行为，早改成暂定读音 + `lt-pending` 了，照实改；
- 词典条数从 6057 改成 6077，并把「制作信息行 / 默认页翻译层跳过」写进去。

回归测试：`tests/integration.test.js` 新增一例钉住预览的七个读音 + 翻译行不注音；
全套 **186 → 187 全绿**。

### 预览图换成真机截图（本次）

- `src/preview.png` 改成用户提供的真机截图（网易云歌词页，《快晴》那句
  「I know this sky loves you.」上面标着 アイ ノウ ディス スカイ ラブズ ユー）——
  原来那张是 `tools/make-preview.js` 画出来的示意图（480×240、2 KB），
  示意图看不出插件在真机上的样子，README 头图也一直不好看。
- 顺手删掉那个生成器（含 `npm run build:preview`）：它的输出就是覆盖
  `src/preview.png`，留着只会在哪天误跑一次、把真机截图换成占位图。
  截图本身进了插件包（`manifest.json` 的 `preview`），包体 0.14 MB → 1.34 MB，
  相对插件包本身可以忽略。

### 网易云默认歌词页：翻译层和「编曲」行不再被注音（本次）

用户报的：默认歌词页里的**翻译**和**编曲**也被注上了片假名。

定位（都有真机证据，不是猜的）：

1. 默认页的结构是 `ul#mod_pc_lyric_record.lyric > li.line > p × 2`
   （第一个 `<p>` 原文、第二个是**中文翻译**）—— 这条来自 jp-furigana 源码里标着
   「NCM 3.1.36 实测」的注释，跟我们自己轨迹里的「默认页的クローバー」对得上。
   而我们的歌词选择器把 `ul.lyric > li` 排在**最前**，于是整个 `<li>` 成了区域，
   翻译那一块是它的子节点，跟着原文一起被注了音。
2. 制作信息正则里只有 `作[词詞曲編编]`（作词/作曲）——**`编曲` 从来不在名单里**，
   所以「编曲 : Kenji」的 Kenji 照标；标签和名字分属两个 `<p>` 时，
   名字那一块连"行首关键字"都看不到，更拦不住。

改法：

- 歌词选择器改成**越细的行排越前**：先 `ul.lyric > li > p`，再 `ul.lyric > li`。
  收集区域时新增两条**歌词专属**规则（只对歌词那组选择器生效；播放栏那组是
  「歌名 + 歌手」结构，套用会把歌手丢掉）：
  - **同一个 `<li>` 里只留一块**，判据用**假名**而不是 class（那两层通常没有可用的
    class）：日文原文必有假名、中文翻译一定没有。已经有带假名的一块就扔掉后来者；
    前一块没假名而这一块有，说明前一块是翻译层，换掉它；两块都没假名
    （纯英文原文 + 中文翻译）时留**第一个** —— 网易云原文在前。
  - **不再收已经被更细的行覆盖的祖先**，否则刚踢掉的翻译层会跟着父区域 `<li>` 回来。
- 制作信息正则补齐中文词（编曲 / 編曲 / 母帶 / 製作人 / 出品 / 监制 / 吉他 / 贝斯 /
  键盘 / 弦乐 / 和声…），中文那一支要求后面跟冒号；英文那一支要求跟 `:` / `by` / `-`，
  免得 `Music と light の 中` 这种歌词行被误杀（反向用例也钉在测试里）。
- 顺手把 RNP 3.0.2 bundle 里的实际 class 全捞出来核对，跳过名单补上
  `rnp-lyrics-overview-line-romaji / -translation / -placeholder`（总览页那两种变体
  老正则漏了），并加了通用兜底 `trans / translated / translation / romaji`
  和 AMLL 那类 `lyricSubLine`；同时把 `div[class^="rnp-lyrics-overview-line"]`
  加进歌词选择器 —— 总览页的原文行以前根本没被标。

回归测试：默认页三种 fixtures（原文 + 翻译、纯英文原文 + 翻译、空占位块 + 原文）、
制作信息四种写法、标签与名字分块的编曲行、RNP 总览页三层、以及
`Music と light の 中` 的反向用例 —— 新增 7 个用例，全套 **179 → 186 全绿**。

### 全英文行"标注后消失"：等待在线结果时改用暂定读音（本次）

用户报的：全是英文的一行，标注之后**有概率消失**。

定位到三个机制（都跟上一版"在线优先、规则垫底"的实现方式有关）：

1. **等待期间"先不标"会把整行拖空**：一行里只要有一个词典外的词在等在线结果，
   这行就注不上；而这个词所在的**原文本节点已经有记录了**，等结果回来时这一轮扫描
   会跳过它（记录完好），于是**一行的首个词**永远补不上 ——
   真机复现：`kaleidoscope zephyr serendipity light` 里 `kaleidoscope` 一直是裸文本。
2. **在线结果回来时走 restoreAll + 重注**：等于把整行先清空再补回来，
   清空那一刻如果还有词在等，看到的就是"消失"。
3. `applyBatch` 只在"有命中"时才通知上层重扫 —— 一批全是"给不出"时没人重扫，
   那些词会一直空着（轨迹里实测空了 **2.3 秒**）。

改法：

- 等待在线结果时**不再空着**：用规则读音当**暂定值**，注音加 `lt-pending` 类
  （样式淡到 45%）提示"还不一定"；拿到结果由新增的 `annotate.relabel()`
  **就地改写**（DOM 节点一个都不动，连重建都没有）并去掉标记。
- 在线结果回来时不再 `restoreAll`，改成 `relabel()` + 一次扫描补新词。
- 两层（大模型 / 免费接口）**不管有没有命中都通知重扫** —— miss 也是状态变化，
  规则层该接手了。
- 语境改用**宿主的可见原文**（注音前后不变），不再用文本节点自己的值：
  后者在注音之后会被我们切短，同一个词会算出两个不同的缓存键、被反复问。

回归测试：全英文行的暂定标注 → 拿到结果就地改写（断言**同一个节点对象**、没重建）
→ 模型给不出的词保持规则读音 → 同一个词不重复问。

### Georgette 注成 ゲオーゲターテ：专有名词进人工词表（本次）

用户报的：`Georgette` → ゲオーゲターテ。（顺手确认：输出是**片假名**、不是平假名 ——
`reading.js` 的表里没有平假名混入，我扫过。）

原因很直白：这个词不在词典里，走的是英文拼写规则，而 `geo` / `rge` 这种拼法规则只能瞎猜
（英文的软 g / 硬 g 光看拼写定不下来：`get` 是硬 g、`gem` 是软 g）。
专有名词只能人工核 —— 写进人工词表定死 **ジョージェット**（织物义另有 ジョーゼット）。

**顺带修了首音校验的两个会错杀正确答案的洞**（上一版加的）：

- `think` シンク（th → サ行）、`phone` フォン（ph → ファ行）—— `t` / `p` 开头的白名单里
  原来只有 タ行 / パ行，会把 θ / φ 的正确音译判成"不是音译"直接丢掉；
- `gem` ジェム、`George` ジョージ、`giant` ジャイアント（软 g → ジ行）、
  `chat` チャット（ch → チ）—— `g` / `c` 的白名单里原来没有软音那一档。

现在同一个字母的多行都放行，测试里把这 7 个都钉住了（配上原有的"拦拟声词"用例）。

### Ave Mujica 注成 アベニュー：缩写被展开（本次）

用户报的：`Ave Mujica` → **アベニュー ムジカ**。

定位：词典里 `ave` 那条就是 **アベニュー** —— 大模型把 Ave 当成了地址里的 "Ave."
（avenue 的缩写）展开。`Mujica` 反而是对的（ムジカ，走罗马音）。

- **把 `Ave` 在人工词表里定死为 アヴェ**：它是拉丁语的"万福"，
  Ave Maria = アヴェ・マリア 是通行写法。人工词表优先于生成词表，以后不会再被覆盖。
- 顺手把同一类"缩写被展开"的生成词条列进黑名单（新增 `tools/dict-blocklist.js`，
  构建与测试共用一份，避免"加一条黑名单就把不变量测试搞红"）：
  `avg` → アベレージ、`blvd` → ブールバード。
- 把词典里 **185 条"词短、读音长"的条目全人工过了一遍**：绝大多数是正确的字母名读法
  （abc エービーシー、cpu シーピーユー、dna ディーエヌエー、ceo シーイーオー…），
  错的只有上面这类"缩写展开"。
- 词典 6057 → **6055 条**（人工 505 + 生成 5550）。

### tick 注成 カチカチ：在线答案加首音校验（本次）

用户报的：`tick` 被注成 **カチカチ**。

先定位清楚：**规则层给的是对的**（`tick` → ティック），问题在**在线那层** ——
Google 的 en→ja 对 tick 会回「カチカチ」这种**拟声词**（"滴答声"），
而且它是纯片假名；我们原来的唯一判据"只收纯片假名"**拦不住意译**
（同理模型也可能回「ダニ」＝蜱虫这种名词义）。

三处改动：

- **首音校验**（`reading.js` 的 `looksLikeTransliteration()`，由 main.js 注入给两层）：
  词首是稳定辅音时，音译的首音必然落在对应的行上（t 只能出 タ行、k 只能出 カ行…）。
  `tick` + カチカチ 的首音是 カ → 判为"这不是音译"，丢掉并记成 miss（不再重问）。
  刻意保守：h / w / y / 元音开头，以及 kn / gn / ps 这类带默字的组合**一律不校验** ——
  `write` → ライト、`hour` → アワー、`knife` → ナイフ、`psychology` → サイコロジー 都照收。
- **提示词加一条**：明确"这是音译不是翻译，不要给日语词、不要给拟声词"
  （`tick` → ティック 而不是「カチカチ」、`beat` → ビート 而不是「鼓動」）。
- 顺手把 `tick` / `tock` 收进人工词表（时计声在歌词里常见，词典命中就轮不到在线层）。
  **后来按用户的选择撤掉了**：`tick` 该写 ティック 还是 チック 是个口径问题
  （ティ = [ti] 更忠实、和词典里的 `stick` スティック 一致；チ = [tɕi]「chi」是英和辞典/大模型的老写法），
  所以不进词典，交给大模型按语境决定 —— 拟声词/意译那条防线（首音校验）照旧拦着。

### 缩写 's / 're / 'll / 'd / 've / 'm / n't（本次）

用户报的：`you're` / `I'll` / `it's` / `I'd` 这类注不准。
（`vs.` → バーサス 其实上一版已经修好了 —— 是 `vs` 那条人工词条，重启后生效。）

根因是折撇号那一步：`normalize()` 会把 `'` 去掉，于是

- `I'll` → `ill`（词典里真有 ill = イル）
- `I'd` → `id`（词典里真有 id = アイディー）
- `you're` → `youre`（谁也不认识 → 规则猜成 ヨウレ）

所以缩写必须**排在词典前面**：拆成"词干 + 缩写尾巴"，词干递归走正常路径
（词典 / 罗马音 / 规则），尾巴按音变补上：

| 尾巴 | 读法 | 音变规则 |
| --- | --- | --- |
| `'s` | ズ | 词干以 ト 结尾并成 ツ（イット → イッツ）、以 ド 结尾并成 ズ（キッド → キッズ） |
| `'re` | ア | 词干的长音收掉（ユー → ユ + ア = ユア） |
| `'ll` | ル | I'll → アイル、you'll → ユール |
| `'d` | ド | I'd → アイド |
| `'ve` | ブ | I've → アイブ |
| `'m` | ム | I'm → アイム |
| `n't` | ント | don't → ドント |

词干是规则猜的，来源就记 `rule`（大模型那层照样会接手）；命中词典就记 `dict`。
整词表兜住拼不出来的：`don't` ドント、`can't` キャント（ca 在词典里是字母名 シーエー）、
`won't`、`ain't`、`weren't`、`couldn't` / `wouldn't` / `shouldn't` / `mustn't` /
`hadn't` / `needn't`、`they're` ゼア、`we'll` ウィル、`y'all` ヨール。

### 读音顺序改成「大模型 / 免费接口 > 英文音译规则」，并且带上语境（本次）

用户提的两条，一起做了：

**一、让大模型联系上下文**

- 请求里除了词，还带上它所在的**整句歌词**（条目形状 `[{i, w, line}]`，响应按 `i` 对齐，
  所以同一个词在两句里不会互相覆盖）。提示词里明确要求"同一个词在不同句子里读音不同时
  分别判断"（`read` 在 “read a book” 里 リード、在 “I read it yesterday” 里 レッド）。
- 缓存键从"词"改成"**词 + 那句歌词**"：同一句里的同一个词仍然只问一次，
  换一句才再问一次。只按词缓存的话，先遇到哪句就把哪个读音钉死一辈子。
- 语境折叠空白、截断到 160 字符（不会把整本歌词塞进提示词）。
- 控制台 `LK.display('词')` 没有句子可给，于是另存一份"词 -> 最近一次读音"的索引
  （`peek()`）给它用；**页面注音仍然严格按当前这句的语境取**，不会串味。

**二、层序反过来：在线优先，规则垫底**

以前的顺序是「词典 -> 罗马音 -> **规则** -> 大模型 -> 免费接口」，规则猜出来的读音会先显示，
等在线结果回来再换掉。现在是：

| 顺序 | 来源 | 行为 |
| --- | --- | --- |
| 1 | 词典 / 字母名 / 记号 | 确定，直接用 |
| 2 | 罗马音 | 确定，直接用 |
| 3 | **大模型**（配了 key） | 优先；等待期间**先不标**，回来再补 |
| 4 | **免费接口**（没配大模型时） | 同上 |
| 5 | 英文音译规则 | 只有 3/4 给不出、失败、或没配时才用 |

代价是词典外的词会晚半秒到一秒才出现（先不标一个错的，好过标错再改），
所以加了一条硬底线：**在线那层挂了/在退避/没配 key 时，`isWaiting()` 立刻返回 false，
规则立刻兜底** —— 断网不会变成一个词都不标。为此还修了免费那层一个小问题：
请求失败时只记 miss、不通知上层重扫，于是"先不标"的那一轮永远补不回来；
现在失败路径也会 `onUpdate()`。

**三、顺手抓到一个真 bug**：`applyBatch` 里有个局部变量 `byWord` 把外层同名的
"词 -> 读音"索引**遮住了**，于是 `peek()` 永远是空、控制台看不到大模型的结果。
是新加的端到端断言把它逼出来的（测试先红，再定位，再修）。

回归测试：`isWaiting` 的四种状态（等待/有结论/miss/退避）、
同一词不同语境分别问分别记、同一句只问一次、语境截断、
"等待期间不标、失败后回落"、"完全离线立刻用规则"。

### 缩写：Mr. / Dr. 念整个词，LDK / TV 这类逐字母（本次）

用户报的：`Mr.`、`Dr.`、`LDK` 等注错了。改之前是规则层硬拼的结果：
`Mr` → **ンー**、`Dr` → **ドー**、`Mrs` → **ンラス**、`LDK` → **ラダク**、
`TV` → **タブ**、`BGM` → **バグム**、`RPG` → **ラパグ**、`DVD` → **ダバド**。

- **缩写进人工词表**（`tools/seed-words.js`）：`mr` ミスター、`mrs` ミセス、`ms` ミズ、
  `dr` ドクター、`prof` プロフェッサー、`jr` ジュニア、`sr` シニア、`vs` バーサス、
  `etc` エトセトラ、`ltd` リミテッド、`inc` インク —— 日语里念的是整个词，不是字母名。
  有歧义的没收（`St.` セント/ストリート、`No.` ナンバー/ノー）。
- **全大写无元音缩写逐字母读**（新的 `spellOutAcronym()`）：`LDK` エルディーケー、
  `NHK` エヌエイチケー、`CD` シーディー、`TV` ティーブイ、`BGM` ビージーエム、
  `RPG` アールピージー、`DVD` ディーブイディー。判据刻意收得紧，免得误伤真词：
  原文**全大写** + 长度 2~6 + 一个元音都没有，而且 **y 也算元音**
  （否则 my / sky / why / fly 会被逐字母念）；小写感叹词（hmm / tsk / shh）
  和混大小写（Ldk）都不受影响。
- 词典里已有的缩写仍然走词典（`CM` シーエム、`DJ` ディージェイ），互不冲突。

### 带变音符号的拉丁字母（Ō / é / ü …）（本次）

用户报的：`Ō` 等没注音上。根因是 matcher 的字符集只有 ASCII `[A-Za-z]` ——
后果不只是漏标：`Tōkyō` 会被切成 `T` + `ky`，而 `ky` 单独命中词典读成 **ケーワイ**，
比不标还糟。

- matcher 的字符集扩到 Latin-1 Supplement / Latin Extended-A、B / Latin Extended
  Additional，扫描循环的**首字符判断**也跟着改（只改正则不改这里的话，
  `Ōkami` 会在 Ō 上直接跳过，只剩 `kami` 被当成一个词）。
- 读音层新增 `foldLatin()`，分两组处理：
  - **长音符 ā ē ī ō ū**（日语罗马字的写法）折成「元音 + `-`」，而本仓库的罗马音
    口径里 `-` 就是长音 → `Tōkyō` **トーキョー**、`kōhī` **コーヒー**、
    `arigatō` **アリガトー**、`Ō` **オー**。这一组排在词典**前面**
    （长音符是日语罗马字的标志，按罗马音读更贴近唱出来的音）。
  - 别的变音符号折成基础字母（`Café` → cafe、`déjà` → deja）：词典优先
    （`Café` **カフェ**、`José` **ホセ**），词典没有的标 `confident: false`
    交给大模型/联网（读音取决于语种，José 是 ホセ 不是 ジョセ）。
- 顺手挡掉一个折叠后才露出来的坑：折叠写法**不能**再走"去掉非字母"那一档词典键，
  否则 `déjà` 会被削成 `dj`，正好命中词典里的 DJ → **ディージェイ**。
- 大模型层的缓存键也折变音符号（`Tōkyō` → `tokyo`）：原来 `/[^a-z]/` 会把它削成
  `tky`，缓存键对不上，还会拿半个词去问模型。
- 测试：matcher 字符集与单字母规则、折叠表、长音符/其它变音符的读音与优先级、
  大模型键、以及一整行的端到端（`Tōkyōの夜を arigatō と歌う`）。

### 诊断：怎么看大模型有没有生效（本次）

用户问「如何判断大模型是否生效」。顺手补了三处，并修掉一个**文档骗人**的问题：

- **`LK` 短别名根本没挂**：启动日志、README、排障文档写的都是 `LK.stats()`，
  但代码里只挂了 `window.LatinKatakana` —— 照着敲会 `LK is not defined`。
  现在两个名字都挂（同一个对象）。
- **`LK.llm.check()`**：把 stats 的数字翻译成人话 + 下一步该做什么。
  重点是回答最容易误会的那一条：`请求 0 次` 既可能是"没填 key"，
  也可能是"歌词里的词全在词典里、这层没活干" —— 现在会分开说。
- **`LK.display(word)`**：页面上**实际用的**读音（可能被大模型换过），
  和 `LK.read(word)`（本地那几层）对照就能看出某个词是谁给的。
  配合 `LK.dict()['词']` 先确认它在不在词典里（词典命中是终点，不会走大模型）。
- 文档里补了一节「怎么判断大模型有没有生效」（四级判据 + 常见误会），
  并把轨迹里的 `[llm]` 行也列进排障清单。
- 测试：`tests/integration.test.js` 新增两条（别名/check 的四种结论、
  display 与 read 的差异），顺手让测试桩把免费那层的 GET 请求也拒掉，去掉噪音。

### 记号逐字母读（本次）

用户要的：`D/N/A` 这种记号按字母名念出来（**ディーエヌエー**）—— 既不漏标，
也不能把里面的 A 当英文冠词读成 ア。

- `core/latin.js` 先认**记号**：字母被分隔符一个个串起来的整体算**一个词**
  （`D/N/A`、`N/A`、`A.B.C`、`R&B`、`X-Y`）。判据是"每一段都是单个字母"，
  所以 `e-mail` / `x-ray` 仍是普通词，不会被逐字母念。
- `core/reading.js` 新增字母名表与 `notationToKatakana()`，读音来源记作 `letters`
  （reader 的 stats 多一项 `letterHits`）；分隔符不发音，`&` 读 アンド
  （`R&B` → アールアンドビー）。
- **记号必须排在词典前面**：`dna` / `abc` / `na` 这些词条就在词典里，
  晚一步就会读成单词音（`N/A` → ナ）。测试里专门钉住这条。
- 注音层和设置面板的预览改成把**原始写法**交给读音层 —— 折过的形式会把连字符吃掉，
  `X-Y`（记号）和 `x-ray`（词）就分不出来了。
- 字母名用通行读法：G ジー、H エイチ（不是 エッチ）、Z ゼット。

### 记号里的单字母不注音（本次）

用户报的：`だって D/N/Aじゃ 騙れない` 里那个 **A 被注成了 ア**。
上一版刚把 `a` / `I` 放行，于是 `D/N/A` 被切成 D、N、A 三个单字母记号，
其中 A 命中了白名单、按英文冠词读成了 ア —— 但它其实是标题记号的零件。

现在 `scan()` 会给单字母打一个 `glued` 标记：**紧挨着的前后一个字符是分隔符**
（`/ \ | _ . -`、全角斜杠、中点，以及 `& # * ~ + =` 这类装饰符号）时，
它算"粘在记号上"，不标。验证：

| 输入 | 结果 |
| --- | --- |
| `だって D/N/Aじゃ 騙れない` | 一个字母都不注 ✓ |
| `N/A` / `A.B.C` / `X-Y` / `&A&` | 不注 ✓ |
| `A story` / `Tell me a story` | A → ア ✓（句首冠词照标） |
| `I love you` | I → アイ ✓ |

代价：`A.` 这种"字母紧跟句号"的写法不再注音（比把 `A.B.C` 的 A 注成 ア 划算得多）。

### 换歌：上一首的歌词不会再留在下一首里（本次）

用户报的：「下一首歌会出现上一首歌的歌词」。网易云的歌词列表会**复用同一批节点**，
换歌时只把文本节点的值换成新歌词 —— 而我们的记录里还攥着上一首的原文，两个坑同时踩了：

1. **跳过条件太粗**：那一轮为了修「RNP 把我们的 `<ruby>` 抹掉、底字没变」，
   把判断写成了"注音还在就跳过"。注释里其实写着「底字没变 ≠ 注音还在」，
   但反过来"注音还在 ≠ 这行还是我们的"：换歌之后我们的 ruby 还挂在原地，
   于是每轮都跳过，**上一首的注音永远留在新歌的行里**。
   现在两条一起看：`annotationsIntact(rec) && !isStale(rec, node)`。
2. **还原会写回旧文字**：`restoreRecord()` 无条件 `node.nodeValue = rec.plain`，
   换歌后那个节点的值已经是新歌词了，写回去就是把**上一首的歌词搬进新歌的行里**，
   而且框架认为这行没变、不会自己修回来。现在先确认"这行还是我们注的那句话"
   （`kept=true` 看原节点的值是否仍是我们写下的那截前缀；`kept=false` 看注音还在不在
   且宿主可见原文没被换过），不满足就**只摘掉我们插的节点，DOM 里的文字一个字都不改**。

顺带修掉一个被上面那道粗闸掩盖的老问题：`isStale()` 拿**整个宿主的可见文本**去和
**单个文本节点的原文**比，宿主里只要有别人的注音底字（`kt-ruby` 里的 コーヒー 这种）
就永远不相等 —— 结果每轮都判"馊了"→ 还原 → 重注。现在记录里存一份
"注入当时宿主长什么样"（`hostPlain`）按它比，`tests/annotate.test.js` 里那条
"别人的注音不算底字"的用例正是靠它才成立的。

回归测试（两条，都是照着用户的现象写的）：换歌后新歌词必须原样留着、旧注音撤干净、
新歌里的词要能重新注上、而且不能被当成打架而认输。

### 读数准确率：单字母词 `a` / `I` 也标（本次）

用户报的：`Tell me a story tell me a story 叶うなら` 这一行里，其它词都注上了，
只有 `a` 空着 —— 那是早期"单字母一律不标"的规则干的（当初是为了躲开首字母缩写
和排版噪声）。可 `a` 和 `I` 是**真正的英文单词**，在 J-pop 歌词里满地都是，
漏掉它们比标错还显眼。现在：

- `looksReadable()` 放行 `a` / `I`（`I` 归一化后是 `i`），其它单字母照旧不标；
- 词典里补上这两条（`a` → ア、`I` → アイ，人工词表 493 条）；
- 端到端用例把用户那一行原样钉住：`Tell テル / me ミー / a ア / story ストーリー`；
- 已知取舍：罗马音歌词里孤零零一个 `i`（= い）会被读成 アイ —— 但 RNP 的罗马音层
  本来就整层跳过，纯罗马音行里的裸 `i` 极少，而英文部分的 "I" 远比它常见。

### 规则层重写：按 sljfaq 的约定

英文音译规则（`src/core/reading.js` 的 `convertEnglish`）改成按
sci.lang.japan FAQ 的
[How do I write an English word in Japanese?](https://www.sljfaq.org/afaq/english-in-japanese.html)
实现，代码里每条规则/每张表都注了 `参照 sljfaq：…`。改动要点：

- **补齐了参考页点名的规则**：非重读 r（car/bird/horse/park）、
  θ/ð（think/the/-ther）、`ti/di` → ティ/ディ、`dz` → ッズ、
  `æ after k` → キャ、辅音 + 词尾 y → イー、`-Cle` → クル/プル/…、
  m/n 在辅音前收 ン、单音节词词尾塞音促音、`-ture` → チャー、`-ous` → アス。
- **v 从 ヴァ行改回 バ行**（参照 sljfaq：バ行是首选写法，love → ラブ、
  vitamin → ビタミン）；ヴィ 只在日语实际那么写的词里保留（visual）。
- **促音保守化**：参照页面的说明「多音节词只在重读音节促音」，
  而重音光看拼写定不下来，所以只对单音节词的词尾塞音补 ッ，
  多音节词交给 `confident: false`，不猜。
- **删掉了「声门塞音式」的旧表**（`EN_UNSURE` 里的 th/wh/gh 一律标不放心）：
  改成按拼写歧义清单（`EN_UNSURE_RE`）判断，
  θ/ð 的**读音**照页面给（シ / ザ），只有「哪个词是 θ」拼不出时才标不放心。
- **硬契约加严**：`englishToKatakana()` 的输出保证是纯片假名（ァ-ヶ + ー），
  非法字符统一清理并置 `confident: false`；由 `tests/dict.test.js`
  的不变量测试遍历全部词典词守住。
- **小表/例外表扩充**：新增 phone / sugar / visual / violin / vitamin /
  tourette / moon / moonlight / sunshine / destiny / goodbye / remember /
  together / second / button / cotton / common / hamburger 等
  规则拿不准但歌词高频的词。

**验证**：`npm run test:serial` 全部通过（latin 11 / reading 54 /
sljfaq-words 3 / annotate 9 / correct 10 / integration 13 / patch 8），
`tools/check.js` 通过。新增 `tests/sljfaq-words.test.js`：
把参考页的例子分成「读得对」与「读不出来（逐条附理由）」两张表，
页面上出现的例子必须登记，不许静默跳过。

**已知缺口**（拼写决定不了，页面自己也说先查词典）：
`ow`（now/snow）、`oo`（book/moon）、`oo+r`（door/four）、`our`（hour/tour）、
`θ/ð` 同形、`-er` 的两个读音、`igh` 被 `f` 拼块抢先（fight）、
专有名词（Disney/Washington）、词典形（pajamas/slippers/router/tarot）。
完整清单与理由见 `tests/sljfaq-words.test.js` 的 `KNOWN_GAPS`。

### 读音从哪来（四层）

| 顺序 | 来源 | 例子 | 可信度 |
| --- | --- | --- | --- |
| 1 | 离线词典（491 条） | `clover` → クローバー、`light` → ライト | 确定对 |
| 2 | 罗马音切分 | `sekai` → セカイ、`shinjiteru` → シンジテル | 确定对 |
| 3 | 英文音译规则 | 兜底，永远给得出结果 | 猜的（标记"没把握"） |
| 4 | 联网校正 | Google en→ja，**只接受纯片假名** | 更准 |

词典的 491 条来自两处：把片假名终结者的离线词典反转（那份本来就是
"片假名外来语 → 英文原词"），加上手工补的歌词高频词与**变形词**。

补变形词是实测逼出来的：规则吃的是拼写、不懂英文音变，所以
`shining` → シイニング、`dancing` → ダナシング、`stories` → サトリエス、
`running` → ルッナイング 全是错的，而 J-pop 歌词里恰恰全是这种词。
现在它们直接进词典。

### 标注范围

- 歌词原文行里的拉丁词 ✅（**含汉字的行也照标** —— 拉丁字母跟振假名不是同一批字，
  没有让位的理由；这是和片假名终结者最大的语义差别）
- 播放栏的歌名 / 歌手 ✅
- RNP 的**罗马音层**和**中文翻译层** ❌（本身已经是读音或译文）
- 单字母、制作信息行 ❌

### 与另外两个插件共存

DOM 注入、还原、churn 认输、`age` 闸门、同步修复钩子这套机制是从
片假名终结者那边继承的（都是真机踩出来的），另外做了四件事：

1. 共存补丁的识别范围扩到**两家**：`kt-ruby/kt-rt` 与 `lt-ruby/lt-rt`，
   标记位 `__ktOwned` / `__ltOwned` 都认。少认一家的后果不是"少标一点"，
   而是那一家的注音会让行被判脏并重建 —— 只有它一家闪，极难查。
2. 修复钩子**链上去而不是覆盖**：`window.__ktRepairLine` 可能已经被
   片假名终结者挂了，直接赋值会把它顶掉，那边立刻开始闪。
3. 片假名终结者也加了互认（2.1.1）：它的"可见原文"现在排除三家注音
   （`kt-rt` / `lt-rt` / `fg-rt`），否则内核不支持 ruby、我们降级成
   `<span class="lt-rt">` 时，它会把我们的片假名当成"底字变了"。
4. 反过来我们也认它们：`isSkippable()` 会整棵跳过 `kt-ruby/kt-rt` 与
   `fg-ruby/fg-rt`。片假名终结者的注音里装的偏偏是**英文原词**
   （`<rt class="kt-rt">dream</rt>`），正是我们要标的对象 —— 不跳就会
   在英文注释上面再注一层片假名，等于给注解做注解。
   这条同样只在"内核不支持 ruby、两家都降级成 `<span>`"时才需要 class 兜底，
   所以 `tests/integration.test.js` 里两条路径各钉了一个用例。
5. 片假名终结者的同步修复钩子也改成**链式**（2.1.2）：它原先直接覆盖
   `window.__ktRepairLine`，谁后加载谁说了算。如果它加载在我们后面，
   我们就被顶掉、收不到同步通知（于是只剩 MutationObserver 那条"晚一帧"的路）。
   现在两个方向都能链，加载顺序不影响功能。

### 工具链

- `tools/make-preview.js`：零依赖手写 PNG 编码器，生成 `src/preview.png`
  （`npm run build:preview`）。这张图之前是从隔壁「片假名终结者」误拷过来的，
  内容对不上，现在是本插件自己的示意图。
- `tests/patch.test.js`：把共存补丁的自检搬进本仓库，并补上本项目独有的回归点 ——
  补丁必须**同时认出两家**插件（`kt-ruby/kt-rt/kt-ov-label` 与
  `lt-ruby/lt-rt/lt-ov-label`），`__ktTextIsOurs` 必须同时接受 `__ktOwned`
  和 `__ltOwned`。这两个断言是真的把补丁写进去的 helper 切出来执行，不是字符串比对。
- `tools/check.js` 的测试文件清单加上新文件，并且同时校验 `test` 与 `test:serial`
  两个脚本都登记了它（沙箱里唯一跑得通的是 `test:serial`，漏登记会完全看不见）。
- `tools/verify-install.js`（`npm run verify:install`）：把装到 `C:\betterncm\plugins`
  的那个包解开，逐个文件和仓库 `src/` 做**逐字节**比对，顺带核对版本号、
  注入清单与两个共存关键串。`.plugin` 是复制过去的，"改了代码但真机没变化"
  这种坑不用再靠猜。

### 验证

- 单元测试 **136 个全过**：拉丁词扫描 11 / 读音引擎 54 / sljfaq 例子 3 /
  词典纪律 5 / DOM 注音 9 / 联网校正 10 / 大模型层 21 / 集成 15 / 共存补丁 8
- `npm run check` 通过（0 警告，9 个测试文件全部登记）；读音词典 **6046 条**，
  键是英文、值是纯片假名
- 规则层不变量：词典里全部 6046 个词过一遍规则，输出必须全是纯片假名
- CI 覆盖：静态自检 + 全部测试 + 打包 + 打 tag 发 Release

### 已知限制

- 规则音译只是兜底：`hello` → ヘッラオ 这种还不准；词典覆盖了词频前 6000，
  其余交给大模型（配了 key）或免费接口
- 规则层有几处比老版本更差的拼写（`my` → マイー、词尾 `-se`、二合字母 + r 等），
  这些词全在词典里，真机走不到；清单见 README「已知限制」
- 人工词表里有 2/80 条是"意译"而非"音译"（`cigarette` → タバコ）
- 罗马音路径按 `di → ヂ` 切分，`diorama` 走罗马音会得到 ヂオラマ（走词典才是 ジオラマ）
