# 更新记录

## 0.1.0

首个版本：给日语歌歌词里的**拉丁字母**标注**片假名读音**。

### `YY` 标字母名 + 成串的大写单字母读字母名（本次）

上一轮留的两个问题，用户拍板了：

**一、`YY` 要标。** `合言葉は「YY」` 的 `YY` 现在读 **ワイワイ**（`lt-src-letters`）。
代价说清楚：`XX`（打码）和 `YY`（缩写）拼写**一模一样**、意思相反，本地分不出来 ——
按用户的选择统一按字母名标，所以 `きみがひとり“XX”してるの` 里的 XX 也会读成
エックスエックス。边界保留在"大小写 + 长度"上：

- **全大写 2~3 个** → 标（`YY` ワイワイ、`XX` エックスエックス）；
- **小写**（`xx`）/ **4 个以上**（`XXXX`）→ 仍然留白（打码/噪声）；
- **元音串**（`AAAAA` / `OOO`）→ 按那个元音叠出来（上一轮做的，不受影响）。

**二、同一行里成串的大写单字母 → 字母名。**
`(A, B) 退屈に打つ QTE (Why?)` 现在读 **エー / ビー**，`QTE` キューティーイー。
判据是**整行**（`main.js` 的 `lineLetterRun`），只认两种形状：

- 被标点串起来的：`(A, B)`、`A・B`、`A&B`；
- 三个以上孤立的：`A B C`。

**不能用"这行有两个单字母"**：`A story of love and I` 也有两个（冠词 A、代词 I），
那样 A 就被读成 エー 了 —— 现在它仍然是 **ア**（测试锁着）。
孤零零一个大写字母（`B面`）照旧留白，`a` 以外的**小写**单字母也照旧不标。

测试：`latin.test.js` +1 与改写 1 条（大写 2~3 个重复字母要标、小写/长串留白、
大写单字母放行）、`integration.test.js` +1（`YY` 与 `(A, B)` 逐项核对 +
冠词 A 反向断言），全套 **285 → 287 全绿**。

### `KiLLKiSS judy / jude / juda` 与乐队名 `Ave Mujica`（アベ ムジカ）（本次）

用户截图（Ave Mujica 的歌）三行歌词，外加一行文字指出乐队名的读法。

| 词 | 原来 | 现在 |
| --- | --- | --- |
| `judy` | **ジュダイー** ✗（规则） | **ジュディ** ✓ |
| `jude` | **ジュデ** ✗（罗马音层切成 ju-de） | **ジュード** ✓ |
| `juda` | ジュダ ✓ | ジュダ ✓ |
| `KiLLKiSS` | **キララキス** ✗（规则） | **キルキス** ✓ |
| `Ave` | アヴェ（按拉丁语"万福"定的） | **アベ**（按用户口径：乐队官方读法就是 **アベ ムジカ**） |
| `Mujica` | ムジカ ✓ | ムジカ ✓ |

**规则层新增词尾 `-ude`（第四批通例）**：那个 e 不发音、u 是长音 ——
`jude` ジュード、`rude` ルード、`gratitude` グラティテュード、
`solitude` ソリテュード、`magnitude` マグニテュード、`interlude` インタールード；
`du`/`tu` 按日语惯例读 デュ/テュ（`dude` デュード、`attitude` アティテュード）。
罗马音层也拒绝这个形状（否则 `jude` 永远被切成 ジュデ）——
顺带把 `-ize`/`-ude` 的拒绝条件从"词干至少一个字母"放宽到"词干可以为空"，
这样 `size` / `prize` 这类词根就是 `-ize` 的词也走规则（サイズ / プライズ）。

**`Ave` 改口径**：之前定成 アヴェ（Ave Maria = アヴェ・マリア 是通行写法），
用户指出乐队 `Ave Mujica` 的官方读法是 **アベ ムジカ** —— 它是歌名/歌手栏里
反复出现的那一个，按用户口径改成 アベ（`Ave Maria` 写成 アベ・マリア 也通行）。

词典 6337 → **6347 条**（新增 judy / jude / juda / killkiss / mujica / rude /
gratitude / solitude / magnitude / interlude 等）。

测试：`reading.test.js` +1（`-ude` 不走罗马音层 + 6 个词读音 + 词干为空的 `-ize`）、
`integration.test.js` +1（三行 KiLLKiSS 与乐队名逐词核对，并改掉老的 Ave 断言），
全套 **283 → 285 全绿**。

### 七张截图：缩写（SOS/QTE/AT）、喊叫（AAAAA）、署名行的碎片（本次）

用户一口气发了七张图。逐条核过之后，**四个是错的**，都修了：

| 截图 | 原来 | 现在 |
| --- | --- | --- |
| `対バンにはATフィールド` | `AT` 读成词典里的英文词"at" → **アット** ✗ | 标成"没把握"交给大模型按整句判 → **エーティー** ✓（模型没开时显示还是 アット，不会更差） |
| `空中散歩のSOS` | **ソス** ✗（罗马音层抢的） | **エスオーエス** ✓ |
| `邪魔者は成敗いたAAAAAす！` | `AAAAA` 一个音都没有 ✗ | **アアアアア** ✓（元音串＝喊叫） |
| `(A, B) … QTE (Why?)` | `QTE` 读成 **クテ** ✗ | **キューティーイー** ✓ |
| `混音&母带处理：宫奇Gon` / `制作人：蔡近翰Zoe` | 前者的 `Gon` 被注音、后者整行跳过 —— **同一类行两种结果** ✗ | 两行**整行都不注音** ✓ |

**一、全大写缩写把"有元音的"也收进来**（`SOS` / `QTE` / `YY`）。
原来是"全大写 + 无元音"才算缩写，于是 `SOS` 落到罗马音层成了 ソス、`QTE` 落到
规则层成了 クテ。现在加了两次判断，**缺一不可**：

- **不是词典里的词** —— `LOVE` / `OK` / `MY` / `YES` 这些真词全大写也常见，不能念字母；
- **罗马音也切不出来** —— `SORA` / `KIMI` / `YUKI` 这类全大写日语罗马字要留给罗马音层；
- 再补一条"不在英文常用词表里"（`MY` / `WHY` / `SKY` / `FLY`）。

`AT` 单独说：它**在词典里**（英文介词 at → アット），所以不适用上面那条。
改法是**加一道"贴假名"的判断**：全大写 2~3 字母紧挨着假名（`ATフィールド`、
`のSOS`、`BGMオン`）就标成"没把握"，让大模型按整句判 —— **只调把握、不改读音**，
所以模型没开时显示的还是原来的词音（不会更差），模型判完还会被自动沉淀成离线词条。

**二、元音串是喊叫，不是打码**。`AAAAA` / `OOO` / `aaa` 现在按那个元音叠出来
（アアアアア / オオオ），走形态层、算确定答案（不会去问模型）。
辅音串（`XX` / `YY`）**仍然一律留白** —— 你要的 `XX` 不注音就是这条。
`YY` 这次还是没注音：它和 `XX` 拼写一模一样、意思完全相反（一个缩写一个打码），
本地判不出来 —— 想要的话我可以让它也走字母名，但那会让 `XX` 又变成エックスエックス。

**三、署名行的碎片**。`混音&母带处理：宫奇Gon` 被 RNP 拆成两个节点，判据只看
节点自己的文字，`Gon` 就不像署名了。两处修：① 判据往上找到最近的
`li` / `.rnp-lyrics-line`，用**整行**的文字判（并且允许 `混音&母带处理：` 这种
组合写法）；② 认出来之后记一笔跳过原因，排障时看得到。
反向守住：`Music と light の 中` 这种只是带词头的正常歌词照标（老测试＋新测试都守着）。

测试：`latin.test.js` 改写 1 条（辅音串不标 / **元音串要标**）、
`reading.test.js` +2 与改写 1 条（有元音缩写逐字母、SORA/LOVE/MY 不受影响、
元音串叠出来）、`integration.test.js` +1（七张图里那五类逐项核对，含"AT 要问模型、
SOS 与署名行不许问"），全套 **280 → 283 全绿**。

### `memorize` / `relentlessly` / `awake`：-ize 一族 + 词表外常用词（本次）

用户第二张截图：`Every night brings a dream but the day, relentlessly, keeps me awake`
—— **读音都对**，但查下来 `relentlessly` / `awake` 是大模型临时给的：
**离线时分别会读成 レレントレスライー ✗ / アワケ ✗**（后者是**罗马音层**抢的：
a-wa-ke）。加上上一张图的 `memorize` → メモリゼ ✗，一共两类问题：

**一、词尾 `-ize` / `-yze` 是一整类漏洞。**
罗马音层的判据只是"整串切得干净"，`memorize` = me-mo-ri-ze 切得干净，就被它
**confident 地**读成 メモリゼ —— 它排在规则层前面，于是模型没机会纠、离线也错。
可日语罗马字里**根本没有 `-ize` 结尾的动词**，所以：

- 罗马音层现在**直接拒绝**这个形状（`RE_ENGLISH_IZE`），让规则层接手；
- 规则层新增 `EN_IZE`：词干照读，尾巴统一「辅音 + イズ」，**辅音并入 a**
  （memorize = メモ + ラ + イズ = メモライズ），软音 g/c 走 ジャ/サ
  （apologize アポロジャイズ、criticize クリティサイズ），变形一起管
  （memorized メモライズド、memorizing メモライジング、analyze アナライズ、
  paralyze パラライズ、organize オーガナイズ、fantasize ファンタサイズ）。
  （自己踩的坑：尾巴要写「イズ」不是「アイズ」，否则成了 メモラアイズ —— 测试里锁住。）

**二、词干本身读不准的、以及 `-ise` 拼法的，人工进词表**（词典 6315 → **6337 条**）：
`memorize` メモライズ、`memorized` メモライズド、`memorizing` メモライジング、
`memorable` メモラブル、`realize` / `realise` リアライズ、`realizing`/`realising`
リアライジング、`realised` リアライズド、`idolize` アイドライズ、`mesmerize` メズマライズ、
`socialize` ソーシャライズ、`sympathize` シンパサイズ、`energize` エナジャイズ、
`advise` アドバイズ、`precise` プリサイス、`surprising` サプライジング；
截图那一行的 `every` エブリ、`relentless` リレントレス、`relentlessly` リレントレスリー、
`awake` アウェイク、`awaken` アウェイクン、`asleep` アスリープ。
（`-ise` 为什么也走词表：那一支两可 —— `promise` プロミス vs `surprise` サプライズ。）

**顺带记一笔已知小缺口**：英文常用词表（`core/enwords.js`，罗马音层判"像不像英文词"用的）
来自 google-10000，`awake` / `memorize` / `relentless` 这类**不在那份频次表里**，
所以那份守卫对它们不生效 —— 这一版靠"规则层修 + 词表补"绕过；
真遇到词表外的词，大模型纠正一次之后会被**自动沉淀成离线词条**（见上一节），越用越准。

测试：`reading.test.js` +1（`-ize` 不再走罗马音层 + 9 个词的读音）、
`integration.test.js` +1（截图那一行逐词核对 + 断言这些词都是确定值、来源是词表或规则），
全套 **278 → 280 全绿**。

### 音乐术语整批进词典（`Lento, presto, andante larghetto`）（本次）

用户发的截图：`(Lento, presto, andante larghetto)` 标的是 レント / プレスト /
アンダンテ / ラルゲット。

**四个读音都对**，但查下来问题在别处：这四个词**词典里都没有**，截图里之所以对，
是大模型临时给的 —— **离线时 `presto` 会被规则读成 プレサト、`larghetto` 读成
ラーーエタト**，而且每听一遍都要重花一次请求。再一查，整个音乐术语区在词典里
几乎是空的（只有 `tempo` / `opera` / `symphony` 这种通用词），而这一区的读法
**唯一**（日语音乐术语有固定写法）—— 正是该人工钉死的类型。

这一版把 **75 个**音乐术语钉进人工词表（词典 6240 → **6315 条**）：

- 速度：`lento` レント、`presto` プレスト、`prestissimo` プレスティッシモ、
  `andante` アンダンテ、`andantino` アンダンティーノ、`larghetto` ラルゲット、
  `adagio` アダージョ、`allegro` アレグロ、`allegretto` アレグレット、
  `moderato` モデラート、`vivace` ヴィヴァーチェ、`largo` ラルゴ、
  `ritardando` リタルダンド、`accelerando` アッチェレランド；
- 力度：`forte` フォルテ、`fortissimo` フォルティッシモ、`pianissimo` ピアニッシモ、
  `mezzo` メッゾ、`mezzoforte` / `mezzopiano`、`crescendo` クレッシェンド、
  `decrescendo` デクレッシェンド、`diminuendo` ディミヌエンド、`sforzando` スフォルツァンド；
- 奏法：`legato` レガート、`staccato` スタッカート、`dolce` ドルチェ、
  `cantabile` カンタービレ、`pizzicato` ピッツィカート、`glissando` グリッサンド、
  `vibrato` ビブラート、`portamento` ポルタメント、`tremolo` トレモロ、`marcato` マルカート、
  `tenuto` テヌート、`arco` アルコ、`tutti` トゥッティ、`fermata` フェルマータ、
  `maestoso` マエストーソ、`giocoso` ジョコーソ、`tranquillo` トランクィッロ、
  `animato` アニマート、`rubato` ルバート、`ostinato` オスティナート、`cadenza` カデンツァ；
- 曲式/体裁：`minuet` メヌエット、`scherzo` スケルツォ、`rondo` ロンド、
  `toccata` トッカータ、`fugue` フーガ、`etude` エチュード、`nocturne` ノクターン、
  `waltz` ワルツ、`tango` タンゴ、`rhapsody` ラプソディー、`ballade` バラード、
  `sonata` ソナタ、`concerto` コンチェルト、`quartet` カルテット、`quintet` クインテット、
  `trio` トリオ、`ensemble` アンサンブル、`overture` オーバーチュア、`prelude` プレリュード、
  `aria` アリア、`soprano` ソプラノ、`tenor` テノール、`octave` オクターヴ、`chord` コード、
  `coda` コーダ、`segno` セーニョ、`trill` トリル、`cello` チェロ、`flute` フルート、
  `trumpet` トランペット。

**唯一的例外是 `grave`**：意大利语术语读 グラーヴェ、英语"墓"读 グレイヴ，两可 ——
按老规矩**不收**，留给大模型按整句语境判（测试里也锁了这条）。

测试：`integration.test.js` +1（截图那一行 + 22 个"规则原本读歪"的术语逐个核对读音、
来源必须是离线词典且确定；顺带断言 `grave` 没被钉死），全套 **277 → 278 全绿**。

### 词典纠错：`know` / `notes` 一族的读音（本次）

用户连着问了两句：「know 的读音是否正确」「notes 的读音」。

**结论先说**：`know` → **ノウ** 一直是对的（词典给的、确定值，词典排在模型前面，
所以它既不会被别的层改掉、也不会去问模型）；`note` → **ノート** 也对。
错的是**同族的几个**，都在这版修掉了：

| 词 | 原来 | 现在 |
| --- | --- | --- |
| `notes` | ノート（那是**单数**；同族的 dates デイツ / rates レーツ 都是对的） | **ノーツ** |
| `noting` | ノティン（规则层） | **ノーティング** |
| `bites` / `kites` / `mates` | ビテス / キテス / マテス（把词尾哑 e 当元音读） | **バイツ / カイツ / メイツ** |
| `bones` / `zones` | ボネス / ゾネス（同一个病） | **ボーンズ / ゾーンズ** |
| `footnote` / `footnotes` / `keynote` | フォオタノテ / フォオタノテス / ケイノテ（规则层） | **フットノート / フットノーツ / キーノート** |
| `knowing` / `know-how` | ノーイング（该和 know 一致）／ノワアウ | **ノウイング / ノウハウ** |
| `throwing` / `flowing` / `blowing` | スロウィン / フロウィン / ブロウィン | **スローイング / フロウイング / ブロウイング** |
| `unforgettable` | ウンフォーゲタタブブル | **アンフォーゲタブル** |

**病因是同一类**：`/oʊ/` 写成 ウ 还是 ー（know ノウ / show ショー / snow スノウ /
slow スロー 都通行，判据是**同族保持一致**），以及**词尾哑 e + s / ing**
（`bites` = bite+s、`noting` = note+ing，哑 e 让前面那个元音变长 —— 规则层现在
只在"词尾就是 e"时判长音，所以这类变形词会读歪）。常见变形词基本都在词表里，
人工钉住这一批就够；模型纠正过的词还会被**自动沉淀成离线词条**（见上一节），
所以这类词会越用越准。

测试：`integration.test.js` +2（know 一族、notes 一族逐词核对，并锁住
`know` 的来源必须是"词典 + 确定"），全套 **275 → 277 全绿**。

### 模型答案自动沉淀成离线词条（「学会的词」）（本次）

用户要的：「让运行期模型给的答案自动沉淀进词典」。

**为什么值得做**：大模型那层是按「**词 + 它所在的那一句**」缓存的 —— 同一个词在不同
句子里读音可能不同，所以缓存必须带语境。代价是**换一首歌、同一句歌词再出现一次，
就得重新花钱问一遍**。现在模型答过的词会自动沉淀成离线词条，按**离线词典的优先级**
参与层序，以后不再问模型。

**收词规矩（刻意保守：收错了它就是"离线权威"，模型再没机会纠）**：

- 模型答的读音要**和本地层不一样**（一样就没必要收）；
- 同一个词要在**两个不同的句子**里答出**同一个读音**（只答过一次的可能是"只在这一句
  里才对"，`read` リード/レッド 就是这种）；
- **两可的短音节不收**（`do`/`re`/`mi`/`me`/`mo`/`pi`… 读音本来就取决于那句话）；
- 模型后来给了**别的**读音 → 立刻把已收的**撤掉**（说明它靠语境）；
- 上限 3000 条，超了丢最久没用过的。

**看得到、改得动**：

- 面板「大模型校正」多了一行 `学会的词：N 个（还有 M 个只听到过一次，再听一句就收）；
  本次已用上 K 个（都省下了一次提问）` 和「清空已学会的词」按钮；
- 控制台 `LK.learn.list() / stats() / forget('词') / clear()`，短别名 `LK.learned()`；
- `LK.word('xxx')` 排障时会把"这是学会的词"写在最前面，并给出去掉它的命令；
- 按来源上色时它是**深绿**（`lt-src-learned`，和词典的绿区分开）。

实现：新增 `src/core/learn.js`（注入清单、`npm run check` 的顺序表、测试清单一起更新），
`llm.js` 多一个 `onAnswer(词, 读音, 句子)` 回调，`main.js` 用它记账并在 `localReading()`
里优先返回学会的读音（名次按离线词典算，所以在线层不会再覆盖它）。

测试：`tests/learn.test.js` +9（答一次不收 / 本地层本来就对不收 / 两句不同读音不收 /
模型改口就撤销 / 规范化 / 落盘重启 / 脏数据 / 上限淘汰 / forget 与 clear）、
`integration.test.js` +2（**两个句子答同一个读音 → 沉淀；把 localStorage 搬到新页面
重启后，`fetch` 一次都不许被调用，页面仍读对**；以及只答过一次不收、两可词不收），
全套 **264 → 275 全绿**。

### 英文行不再被当成罗马字行（`me`→メ）+ 不发音字母的词进词典（本次）

用户两张图 / 一个问题，一起处理。

**一、`Knock knock! Let me go in and get the ace` 里的 `me` 被标成 メ。**
病例是**罗马字行规则误判**：那条规则靠"数打架的短词"（≥5 个、短词占比 ≥60%），
而这行本来只有 4 个，可整句里再多一个（`so`/`no`/`you`…）就凑够门槛 ——
于是整行改按罗马音读，`me`→メ、`go`→ゴ…

能分开英文行和罗马字行的不是**数量**而是**拼写**：日语罗马字里没有 θ（`th`）、
没有 `ck`/`gh`/`ph`，也没有 `q` 和 `x`。现在只要一行里出现
`th|wh|ck|gh|ph|q|x`，就直接当英文行、词典读音照用：

- `Knock knock! Let me go in and get the ace, so no, do you know`（6 个打架短词、
  短词 80%，老判据必中）→ 现在 `me` ミー ✓、`go` ゴー ✓、`so` ソー ✓、`no` ノー ✓；
- `PA PI PU PE PO POP UP!` / `MA MI MU ME MO MORE JUMP!` 里一个都没有 ✓ →
  仍然是罗马字行，`PI` ピ ✓、`ME` メ ✓（那正是罗马字行要的）。

**二、`Knock` 被规则读成ナオック —— 用户问「没有歧音的词语能否直接写入词典」。
答案：能，而且就该这么办，这一版一次做了两层：**

- **通例进规则**（`reading.js`）：词首不发音的 `kn-`/`wr-`/`gn-`/`ps-`/`pn-`
  （knock / wrap / gnome / psalm / pneumonia）整组丢掉首字母；词尾 `-mb` 的哑 b
  （comb / climb / lamb / bomb / thumb）也丢掉。没进词典的生词跟着沾光：
  `knack` ナック、`gnat` ナット、`gnaw` ナウ、`wring` リン。
  词中的 `mb` 故意不碰：`number` ナンバー 的 b 发音、`plumber` プラマー 的不发音，
  分不出来，两个都进词典。
- **常用词人工进词典**（`tools/seed-words.js` +40 条，词典 6192 → **6226 条**）：
  `knock` ノック、`knee` ニー、`knit` ニット、`knob` ノブ、`knot` ノット、
  `wrist` リスト、`wreck` レック、`psalm` サーム、`pneumonia` ニューモニア、
  `comb` コーム、`climb` クライム、`lamb` ラム、`bomb` ボム、`thumb` サム、
  `tomb` トゥーム、`dumb` ダム、`plumber` プラマー、`subtle` サトル、
  `island` アイランド、`aisle` アイル、`castle` キャッスル、`listen` リスン、
  `whistle` ウィッスル、`fasten` ファスン、`sword` ソード、`muscle` マッスル、
  `science` サイエンス、`receipt` レシート、`guess` ゲス、`foreign` フォーリン、
  `who` フー、`whole` ホール …。人工词表压过所有层、还带"确定"标记，
  不会再送去问大模型（等于省一次请求）。

判据只有一条：**读法唯一**。两可的照旧不收（`do`/`re`/`mi`/`me`/`mo` 这类
英文读音和唱名/罗马音节都成立的），钉死反而会错 —— 那些交给大模型按整句语境判。

测试：`reading.test.js` +2（词首哑音、词尾 -mb 不许收在バ行上；同时守住
`number`/`amber`/`timber` 词中的 b 要发音）、`integration.test.js` +2
（带 ck/th 的英文行里 `me` 必须是 ミー、罗马字行不受影响；40 个不发音字母词
逐个核对读音与"确定"标记），全套 **260 → 264 全绿**。

### `XX` 被念成 エックスエックス + 「MWAH 一直没注音」：认输判据放宽、诊断一次给结论（本次）

用户两条反馈，一起处理。

**一、`ねえあたし知ってるよ きみがひとり“XX”してるの知ってるよ` 里的 `XX`
被标成了エックスエックス。** 歌词里的 `XX` 是**打码/占位**（原词被隐去），
不是要念出来的缩写 —— 念成字母名既难看又唱不出来。判据落在"**同一个字母重复**的整词"：
`XX` / `XXX` / `AA` 一律不标（`src/core/latin.js` 的 `looksReadable`）。
真实缩写几乎都是不同字母（`TV` / `DJ` / `LDK` / `MC`），照旧逐字母读；
`X-X` / `A-A` 那种**记号**走的是另一条分支（エックスワイ / エーエー），不受影响。
（查下来 `XX` 的 エックスエックス 来自词典里的字母名条目，所以这一刀切在分词层 ——
不管读音从哪一层来都不标。）

**二、面板「排障」查 `MWAH` 只得到「在标注区域里：是 / 已经有注音记录：没有」，
然后就没有下文了。** 这两行只说明"不是漏扫"，原因还得我猜。现在同一次查询里
把 `pass()` 的判据**原样重放**一遍，直接给结论：

- 祖先被整层跳过（罗马音/翻译层、别的插件的注音节点）；
- 这段文字里没有值得注音的词（单字母、`XX` 这类占位符）；
- **认输期**：还剩多少毫秒、已经被抹掉几轮；
- 制作信息行 / 文本太短 / 文本在动（宿主还在抖）；
- 以上都不成立 → "下一轮扫描会补上，急着看就点立刻重扫"。

同一份报告里还把这一段的词**逐词摊开**：`MWAH → ンワー（rule）`，
拿不到读音的直接写 **拿不到读音** —— 「无译文」以前是唯一**不留痕迹**的跳过原因，
现在看得见了。末尾附一句全局计数（已插注音 N 处、认输期文字 M 段）。

**三、顺手把认输判据里两处过狠的地方改了**（很可能就是 MWAH 的真正成因）：

- `CHURN_FAST_MS` 150ms → **50ms**：注音活过 50ms 才被重绘掉的**不算打架**。
  RNP 的逐字行大约每 100ms 重写一次内容，旧门槛刚好把它误判成"真死循环" ——
  三轮就进认输期，那一行在退避窗口里**完全没有注音**；而我们的补注是在
  MutationObserver 回调里做的（赶在下一帧之前），100ms 的空窗根本到不了屏幕：
  这里没有"闪"要治，认输纯属误伤。
- 再次触发的门槛：以前"判过它爱打架之后，每次重试只给 1 轮机会"（`limit=1`），
  于是换歌/拖进度/逐字动画收尾各一次就能把退避再翻一倍、很快顶到上限。
  现在**每次都要在 1.5s 窗口里凑够 3 次**才算打架（真死循环轻松凑够）。
- 退避上限 2 分钟 → **1 分钟**。

测试：`latin.test.js` +1（重复字母不标；不同字母缩写与 `X-X`/`A-A` 记号不受影响）、
`annotate.test.js` +2（活 70ms 才被重绘不算打架；同一拍连抹 4 次仍然认输）、
`integration.test.js` +1（占位符/制作信息行要在报告里说出原因），
全套 **256 → 260 全绿**。

### 简化设置面板：默认只留三块，其余收进「高级设置」（本次）

用户要求：简化一下面板元素。

这些轮下来面板被我自己堆长了 —— 预览 / 开关 / 外观 / 范围 / 读音来源顺序 /
大模型校正 / API 用量 / 排障 / 操作，九块东西一次铺开，还夹着一堆解释性长句
（那些解释现在都在 docs/notes.md 里，面板上不该复读）。

现在**默认只显示三块**：

```
源码仓库 · 反馈问题
开关：启用拉丁字母注音 / 规则没把握时联网校正读音 / 也标注播放栏歌名歌手
大模型校正：用大模型校正读音 + API Key + 测试连接 + 一行状态
预览：The shirt is nine pounds fifteen pence.（下面跟中文翻译）
▸ 高级设置（接口地址 / 外观 / 范围 / 读音来源顺序 / 用量 / 排障）
```

- 其余全部折进 `<details>`（原生折叠、默认收起，点一下才展开），里面仍按
  「接口 / 外观 / 范围 / 读音来源顺序 / API 用量 / 排障 / 操作」分好组；
- 每个提示句都砍到一两行，长解释留在 `docs/notes.md`；
- 实测元素数：**151 个里 106 个收进折叠块**，默认可见的只剩 45 个
  （之前是九块全铺开）。

功能一个没少：所有 `data-k` / `data-a` / 预览 / 状态 / 层序 / 用量 / 排障
都还在原位置（只是挪进折叠块里），测试里那些按选择器取元素的地方照旧命中。

测试：`integration.test.js` +1（默认可见的必须是开关 / 大模型 / 预览这三块，
高级里的东西一个都不能少、分组标题也要对），全套 **255 → 256 全绿**。

### 「纯英文行又没注上音」：查不出来 -> 把诊断搬进设置面板（本次）

用户报的：纯英文行又没注上音。

**先说复现结果**：10 行纯英文（含之前报过的那些）× 3 种配置（离线 / 配了模型 /
罗马音排词典前面）全部正常标上；又按真机形状试了一遍 —— RNP 的 `-original`、
RNP **逐字**模式 `-karaoke`、行首是词的、词被拆进多个 `<span>` 的、
带 `<br>` 的、带翻译兄弟节点的 —— 也全部正常。所以问题只可能出在**用户那一行的
容器**上（不在我们的标注区域里），或者那一行正处在"认输期"。

这类"某一处不注音"最麻烦的是我这边看不到现场，而让用户去开控制台敲命令这一环
一直没走通（他习惯截图）。所以把诊断搬进设置面板：

- 新增**「排障」区块**：填一个词/一段歌词，点「查这一行为什么没注音」或
  「查这个词的读音来源」，结果直接显示在面板上（截图发我即可）。
  这两颗按钮调的就是 `LK.why(文本)` 和 `LK.word(文本)`。
- `LK.why()` 顺带两处改进：**单个节点注音时抛的异常**以前只写进轨迹、现在会回到
  上层并显示出来（"注音时出错（这些行没标上）"）；查的词**已经标上了**时会明确
  说"已经注上了 ✓"（行首是词的那行原文节点被清空、词都在 `<ruby>` 里，
  以前会误报"没找到"）。

另外把**认输期的退避上限从 10 分钟降到 2 分钟**：那一行被播放器/别的插件反复重建
（RNP 逐字歌词会为每个字重建当前行）时我们会主动认输，原来退避一路翻倍到 10 分钟，
那行就"永远"没注音了 —— 用户之前几次"某一行不注音"很可能就是这个。
2 分钟够礼貌，也不会让人以为插件坏了。

测试：`integration.test.js` +1（面板排障区块：空输入提示、已注音的行给明确结论、
没标的行给元素链和「不归我们管」、查词的读音来源），全套 **254 → 255 全绿**。

### Shoo / Gimme / Do / Re / Yeah：词典缺词 + 两可短音节（本次）

用户报的：罗马音节行的问题还在；`Shoo`、`Gimme more` 被**按罗马音读了**；
`「Do」「Re」「Meet」を重ねて` 效果也差；`Yeah` 的读音要改。

**先认一个错**：上一条我建议"把词典和罗马音的顺序对调试试" —— 那句建议是错的。
罗马音层和规则层一样是**"能切成音节就收"**，放到词典前面就会把英文词也按罗马音读：
`more`→モレ、`Shoo`→ショオ、`Gimme`→ギッメ、`Do`→ド、`Re`→レ。
用户看到的正是这个。默认序（词典 > 罗马音）必须保持。

逐条修：

1. **`Shoo` / `Gimme` 词典里根本没有** —— 所以才被罗马音层抢走（ショオ / ギッメ）。
   人工补上 **シュー / ギミー**，顺手把审计里同类的也补了（Harrison ハリソン、
   Mason メイソン、midi ミディ、rouge ルージュ、genome ゲノム、indie インディー、
   dude デュード、emma エマ、fujitsu フジツウ…，词典 6169 → 6192 条）。
2. **`Yeah` → イェイ**（原来词典里写的 イェア；连 `yea` / `yay` 一起钉死）。
3. **两可的短音节交给语境**：`Do`（ドゥー / ド）、`Re`（リー / レ）、`MI`（ミー / ミ）、
   `ME`（ミー / メ）、`PE`（ピーイー / ペ）这类，光看拼写分不出来（`do`/`no`/`me`
   真是英文词，`pi`/`po` 只是被词表收进去了）。现在这些词**标成"没把握"**，
   由大模型按整句语境决定 —— 实测：「Do」「Re」→ **ド / レ** ✓，
   同一批在英文行里仍是 ノー/ユー/ソー ✓，模型给不出来就保持词典读音（离线不退化）。
4. **罗马字行的判据再加一条**：除了"≥5 个打架的短音节"，还要求**整行 ≤3 字母词占 60% 以上**
   （实测用户那行 83%、`Shoo, Gimme more` 0%），免得长词句被误判成罗马字行。
5. **面板提醒**：罗马音排在词典前面时明确警告会有什么代价（Shoo→ショオ、Gimme→ギッメ…）。

测试：`integration.test.js` +3（Do/Re 走语境、Shoo/Gimme/Yeah 用词典、
罗马音在词典前面时给提醒），全套 **251 → 254 全绿**。

### 罗马音节行（PA PI PU PE PO…）：词典是英文词典，读歪了（本次）

用户报的：`Yes, PA PI PU PE PO POP UP!(Hey!!)Yes, MA MI MU ME MO MORE JUMP!(Yeah!!)`
这样的用词典效果很差。

**查下来就是"词典是英文词典"这件事**：这些短音节在词典里是**英文词的读音**，
在一首日语歌的罗马字行里全错 ——

| 词 | 词典（英文） | 罗马音（这首歌要的） |
| --- | --- | --- |
| `PI` | パイ | **ピ** |
| `PE` | ピーイー（把 "P E" 当字母念） | **ペ** |
| `PO` | ピーオー | **ポ** |
| `MI` / `ME` / `MO` | ミー / ミー / モー | **ミ / メ / モ** |

而 `pi/pe/po/mi/me/mo` **全都在英文常用词表里**（`me`/`no`/`so` 真是英文词，
`pi`/`po` 只是被词表收进去了），所以"它是不是英文词"这条路分不开它们。
真正分得开的是**整行的构成**：一行里同时出现好几个"词典读音和罗马音读音打架"的短音节，
那就是罗马字行 —— 英文歌词不会那么写。

规则：一行里 **≥5 个不同的**「≤3 字母 + 能切成罗马音 + 词典有条目但读音不同」的词
→ 这一行的短音节改读**罗马音**，并且**标成"没把握"**（在线层/大模型拿到整句语境
还能改回去，所以英文行不会被读歪；离线时就用罗马音，那正是罗马字行要的）。

阈值是量出来的，不是拍的：

- 3 个 → 英文行 `No, no, no, I need you so`（no/i/so）和 `we can go to the sea`
  （we/go/to）都被判成罗马字行 ✗，`go`/`to`/`no`/`you` 被读成 ゴ/ト/ノ/ヨウ；
- 4 个 → `No, no, no, I need you so` 还能凑到 4 个（no/i/so/**you**）✗；
- **5 个** → 用户那行 6 个 ✓、`PA PI PU PE PO` 5 个 ✓、上面两行英文都 ✗，
  实测英文行的 ノー/アイ/ユー/ソー/ゴー/トゥ/ウィー/シー 全部保持词典读音 ✓。

测试：`integration.test.js` +2（罗马音节行按罗马音读且英文行一个都不动、
这些短音节会被送去问模型），全套 **249 → 251 全绿**。

### 「Love, bluh bluh bluh / MWAH! 一直没注音」：诊断升级 + 答案在区域判定（本次）

用户报的：`Love, bluh bluh bluh` 和 `MWAH!` 一直没注音。

**先说实测结果**：这三个词**读音层完全没问题** —— `Love` → ラブ（词典）、
`bluh` → ブルー（规则）、`MWAH` → ンワー（规则），`readable` 也都是 true；
用真插件在 jsdom 里把这两种行跑一遍，**都正常标上了**。
唯一"刻意不标"的是**同一个 `<li>` 里的第二个 `<p>`**（网易云默认页的翻译层）——
那是有意跳过的（`爱、bluh bluh bluh` / `MWAH！` 这种就属于翻译层）。

所以"没注音"的原因只可能是**那几行没落进我们认的标注区域**，
或者祖先被判定成要跳过 —— 而这件事光看症状查不出来。于是把诊断做成一件事：

**`LK.why('MWAH')`**（以前不带参数只报"上一轮跳过统计"）现在会：
- 在页面里找**包含这段文字的原文文本节点**（我们自己插的注音会过滤掉并计数）；
- 打出**元素链**（`p < li.line < ul.lyric < div.m-lyric < div` 这种，带 class）——
  照着这个就能看出它挂在哪个容器上，也方便我按实际结构补选择器；
- 逐条说明：在不在标注区域里（不在就是"这行不归我们管"）、
  哪个祖先被整层跳过（罗马音/翻译层 / 别的插件的注音节点）、可不可见、
  是不是被当成制作信息行、有没有注音记录、这一段的词切出来是什么；
- 最后报一下当前 scope 和认到的区域数；找不到这段文字也会明说
  （那就可能是在桌面歌词/其它窗口）。

测试：`integration.test.js` +1（区域外 / 翻译层两种情况都要说清 + 元素链 + 找不到的情况），
全套 **248 → 249 全绿**。

### `The user aborted a request.`：是我们自己的超时，而且批次太大（本次）

用户截图：面板告警「请求失败后退避中，还要等 70 秒…最近错误：The user aborted a request.」

**这句错误是插件自己掐的**（`AbortController` 到点 abort），不是 key / 余额 / 网络的问题。
原因是单个请求超时定得太紧：**20 秒**，而一批词要带上每个词的整行歌词当语境，
服务商忙的时候首字节就可能等十几秒 —— 一批 30~40 个词很容易踩线，
然后整批失败、进退避、读数不矫正。三处改动：

1. **超时 20 秒 → 45 秒**：这是"等不到"和"服务商在排队"之间更合理的位置。
2. **批次 40 → 30**：一批越小越快回来（请求数由 20 次/分钟的自限兜着，够用）。
3. **超时之后把批次减半再试，成功自动恢复**（新增自适应上限 `batchCap`）：
   比"退避 60 秒再原样重发同一大批"有效得多 —— 那很容易一直超时。
   下限 5 个，成功一次就恢复原大小（只减不加，不会来回震荡）。
4. **错误信息说清下一步**：现在是
   `The user aborted a request. （我们自己的超时掐的：等了 45 秒还没回来 —— 一般是批次太大或服务商在排队。插件会把批次减半自动重试，不用管它）（这批 30 个词，下次改成 15 个再试）`。

（顺带一提：这一条几乎只在"模型排在词典前面"时才会碰到 —— 那样每个词都要问一遍，
队列上百、批次又大。词典放最上面的默认序里，一首歌通常只有几十个词要问。）

测试：`llm.test.js` +1（超时后批次减半、错误信息说清、成功之后恢复），
全套 **247 → 248 全绿**。

### the → セ / this 变黄 / I'll → イル：同一个原因的第三次报告（本次）

用户报的：`this` 还是黄色（`lt-src-rule` = 规则层），`I'll` 被注成 **イル**。
连同之前的 `the` → セ、"第四首歌读数全都没矫正"，**都是同一件事**：
**层序里「英文音译规则」排到了「离线词典」前面**。

为什么一个顺序能惹这么多事：「英文音译规则」是拼写猜测，**对每个词都给得出答案** ——
排在它下面的层就永远轮不到（它的名次决定了"谁说了算"，一层答了就不再往下走）：

- `the` → 规则层的 **セ**（词典里的 ザ、模型给的 ザ 都进不来）；
- `this` → 规则层（黄），词典里明明是 ディス；
- `I'll` → 缩写的**词干 "I" 也走规则层**，读成「イ」，再拼上 `ll` 尾巴 = **イル**
  （正解是词典的 アイ + ル = アイル）；
- **大模型也不再被咨询**（规则先答了）→ 看起来就是"全都没矫正"。

三处改动：

1. **面板不让这么换了**：`↑↓` 在「规则 ↔ 词典 / 罗马音」这一对上直接禁用，
   鼠标悬停给出原因。真要"只用规则"是控制台 `LK.layers([...])` 的事（文档里有）。
2. **两个地方都会点名**：层序区照旧有挡路提醒（文案补上了 `I'll` 这个后果），
   **「大模型校正」状态区也加了**（读数不对时用户先看那里），
   `LK.word('the')` 里同样会说，并直接给出"点恢复默认顺序"。
3. **首音校验补 `this` → ディス**：词典里 `this` 就写作 ディス，但 `t` 那行只放了
   タ行/サ行/ザ行 —— 于是**连词典自己的写法**都会被判掉（模型给 ディス 也会被丢）。
   现在按"词首是不是 th"单独放行ダ行，`tick` → ダニ（蜱虫）那种意译照样拦得住。

**推荐的层序**（也是默认）：`离线词典 > 日式罗马音 > 大模型 > 免费接口 > 英文音译规则`。
词典命中占绝大多数、根本不需要模型；只有词典外/看着像英文词的才送去问，
请求量能降一个数量级。（把模型提到最上面会变成"每个词都问一遍"，
一首歌几百个词就是几百个请求 —— 上一版修的那个队列卡死就是这么来的。）

测试：`reading.test.js`（this → ディス 放行、tick → ダニ 仍拦）、
`integration.test.js` +1（↑↓ 在"规则 ↔ 同步层"上禁用、手改配置时两个区块都报警告、
`LK.word()` 给出修法），全套 **246 → 247 全绿**。

### 队列会永远躺着一动不动：`schedule()` 在退避里直接 return（本次）

用户给的截图把问题指得非常准：面板写着

> 队列里还有 184 个词在等，本分钟还剩 20 次额度 —— 等一会儿就会矫正。

**队列一大坨、额度却是满的（20/20）** —— 说明过去一分钟里**一个请求都没发**，
而且没有请求在飞。顺着查 `schedule()` 的第一行就是根因：

```js
if (inflight) return;      // 正在请求：它回来之后自己会续上
if (!canAsk()) return;     // ← 这里
```

退避期间 `canAsk()` 是 false，于是那次排期**什么都不做**；而退避结束时
**没有任何事件**会再来叫我们（页面不动就没有新的 `lookup`）——
队列就这么一直躺着，用户看到的就是"读音全都没矫正"，而且额度满着、毫无动静。
（失败路径 `fail()` 里那句 `schedule(cooldownMs)` 同样被这行吃掉，
等于"退避结束后重试"这个意图从来没生效过。）

修法：退避里不再直接 return，而是**排到退避结束的那一刻**再去试
（`d = max(d, cooldownUntil - Date.now() + 50)`）；只有这层根本没开
（没 enabled / 没 key / 没地址）才真的返回。这样失败退避能自己走出来，
一路退到上限（3 分钟）也会自动重试。

配套两处：

- **批大小 30 → 40**：词典外的词多的歌一次能排上百个，批大点少发几次请求。
- **面板数字改成实时刷新**（开着的时候每秒轻量刷一次，关掉自己停）：
  这些数字以前是"打开那一刻的快照"，用户看到「184 个词在等」时可能下一秒就排完了，
  也可能真的卡住 —— 分不清。现在队列在排空的过程能直接看见。
  队列积压时还会多说一句：**「大模型」排在「离线词典」前面 -> 每个词都要问一遍**，
  把词典放回最上面请求量就下来了（词典命中的词本来也不需要矫正）。

测试：`llm.test.js` +1（**退避结束后不靠任何外部事件自己重试**，老代码这里必挂）、
批大小用例改成读配置（不再写死 30），全套 **245 → 246 全绿**。

### 「第四首歌时读音全都没矫正」：模型层停摆了，面板却什么都不说（本次）

用户报的：第四首歌时读音全都没矫正。症状很典型 —— 本地读音照旧、模型一条都没改，
看着就像插件坏了。根因是**模型层整个停摆**，而面板只显示"已注音/命中"这些数字，
没有一句话解释"现在为什么不矫正"。

停摆有五种，现在设置面板的「大模型校正」区块会**直接写清是哪一种**，并给出对策：

| 面板上写什么 | 真实原因 |
| --- | --- |
| 这一层没启用 / 没填 API Key | 这层根本没工作 |
| ⚠ 请求失败后退避中，还要等 N 秒 | 进了**失败退避**（接口抖了 / 撞限流），这段时间所有词都不问模型 |
| ⚠ 最近几次请求都没成功 | 连着几批都失败（比退避更彻底） |
| 队列里还有 N 个词在等 | 只是没排到（模型排在最前面时请求多，撞 20 次/分钟的自限） |
| 有 N 条「问过但没收下」 | 那些词的答案被拒过，是永久 miss（见上一条更新） |

配套的三处改动：

- **「立刻重试」按钮**（新增 `LK.llm.retryNow()`）：清掉失败退避、队列马上重发。
  以前进了退避只能干等（最久 10 分钟），或者去改一下配置来触发重置。
- **退避上限 10 分钟 → 3 分钟**。接口抖一下就有十分钟不矫正，实在太像"插件坏了"；
  3 分钟够礼貌，也能自己恢复。同时 `max_tokens` 2000 → 4000，
  免得一批词回来被截断成坏 JSON（那会被判成整批失败、又进一次退避）。
- **`stats().stalled`**（连续失败 ≥2 且队列还有词）——面板的告警和测试都用它判断"停摆"。

测试：`llm.test.js` +4（max_tokens、退避上限、retryNow 清退避并重发、stalled 判定），
`integration.test.js` +1（停摆时面板给大白话告警 + 点「立刻重试」退避归零），
全套 **240 → 245 全绿**。

### 有些词「大模型一直不矫正」：被拒的答案其实被永久记住了（本次）

用户报的：有些词大模型一直不矫正。

查下来有三个机制，前两个是**设计**、第三个是**真坑**：

1. **离线词典命中的词不问模型**（默认层序 词典 > 罗马音 > 在线 > 规则）。
   词典是人工核过的确定答案，问模型既费钱又可能被改坏 —— 想让模型也过一遍，
   把「大模型」拖到词典上面（这就是那个逃生门）。
2. **排在当前答案后面的在线层不参与**：排版顺序是用户定的，后面的层不发请求也不覆盖。
3. **"问过但没收下"是永久的，而且以前不留原因** ✗ —— 模型当时给的答案被我们判掉了
   （首音校验误伤、回的不是纯片假名、响应格式不对、它干脆没给），那份 miss 会落
   localStorage，**重启也不会重问**，那个词就一直用本地读音。更糟的是缓存里只有
   `{miss:true}`，**没记模型说了什么、也没记为什么丢**，所以「某个词一直不矫正」
   在我这边完全查不出来（只能猜）。

这次的改动：

- **记住拒绝原因**：miss 现在连「模型原话 + 原因（模型没给 / 不是纯片假名 / 太长 /
  没通过首音校验）+ 时间」一起存（也落盘），`LK.llm.rejects()` 直接列出来。
- **可以重试**：新增 `retryMisses()`，设置面板的操作区多了一个
  **「重试没结果的词」**（原来那个只重试免费接口那层，现在两层一起清）——
  只清 miss、保留命中，比「清除校正缓存」温和得多。修完判据之后点它就行
  （比如 `the` → ザ 那次修的，旧结论还在缓存里，不点它不会生效）。
- **`LK.word('那个词')`**：一词体检，把这四件事一次说清 —— 词典里有没有、
  按当前层序在线层有没有资格覆盖它、模型缓存里是什么、被拒的话模型当时说了什么。
  排障一个命令就够了。
- **首音校验再补几个词首不发音的组合**：`mn`（mnemonic ニモニック）、
  `pn`（pneumonia ニューモニア）、`Ng`（Nguyen グエン）——
  这些正确答案以前也会被判掉，同样会变成永久 miss。

测试：`llm.test.js` +3（被拒留痕、retryMisses 之后能再问一次、被拒记录落盘并能在
"重启"后还原）、`reading.test.js` +6 例（mn/pn/Ng）、`integration.test.js` +2
（`LK.word()` 说清整条链子、重试按钮），全套 **235 → 240 全绿**。

### `the` 还是 セ + 换歌时那行还是不注音（本次）

**一、`the` 被读成 セ：首音校验把正确答案拦了**

用户把「大模型」提到最前面，`the` 还是 セ。先用真接口问了一遍模型：
它回的**就是 ザ**（一批 7 个词：`{"1":"ザ","2":"プリテンダー","3":"シェイク",
"4":"キルキス","5":"ジュディ","6":"ジュード","7":"ジュダ"}`）—— 但统计写着
「命中 6，模型没给 **1**」：**唯一被拒的就是 ザ**。

原因在首音校验那张表：`t` 开头允许的行里放了サ行（θ 的 `think` シンク、
`three` スリー），却漏了 **ザ行**（ð 的 `the` ザ、`this` ジ、`that` ザッ、
`they` ゼイ、`there` ゼア）—— 于是模型对的答案被判成"不是音译"丢掉，
回落到规则层的 セ。补上 ð 那一行之后实测 **命中 7/7**，`the` → ザ。
（`tick` → カチカチ 那种拟声词照样被拦，这条校验的存在意义没变。）

**二、换歌时那行不注音：跳过之后没人再扫了**

用户报的：换歌的时候 `KiLLKiSS judy.., …` 还是没注音。

换歌那几下文本在动，注音层按"别追着重注（追就是抽搐）"**跳过这一轮** ——
问题在于**跳过之后必须有下一轮**。而如果换歌后页面不再发生变动（最典型的是
**歌是暂停的**：歌词列表渲染一次就不动了），MutationObserver 就再也不会叫我们，
那一行**永远**空着，直到用户点播放/滚动才补上 —— 看着就是"插件坏了"。

修法：`pass()` 现在会报出**下一次该什么时候重试**（跳过是"文本在动"时 =
滑动窗口剩余时间，是"认输期"时 = 退避剩余时间），`main.js` 据此自己排一轮扫描。
哪怕页面一动不动，3 秒后那行也会自动补上。实测（集成测试）：
连换 4 次 → 当轮全跳过 → 之后**一个 DOM 事件都不发生**，只等 3.6 秒 → 注音自己出现。

**三、顺带两个诊断改进**

- `LK.why()`：一句话回答"这些行为什么没注音" —— 上一轮的扫描/注音/跳过计数，
  加上**跳过原因的归类计数**（文本在动 / 认输期 / 无译文 / 切不出词…），
  还会说明"已安排 N ms 后再扫一轮"。`LK.stats().lastPass` 里也有同样的数据。
- 设置面板的层序加了**挡路提醒**：把「离线词典」或「日式罗马音」拖到
  「英文音译规则」下面时，会提示"规则对每个词都会给答案，这两层就永远用不上了
  （比如 the 会变成规则猜的 セ）" —— 只提醒不阻止，因为"只用规则"也是合法选择。

测试：`reading.test.js` +1（th 的两种读法都放行、拟声词照样拦）、
`annotate.test.js` +1（pass() 报出 retryInMs 与跳过原因）、
`integration.test.js` +2（换歌后页面不动也能自己补上、挡路提醒），
全套 **231 → 235 全绿**。

### `shake` 为什么是 シャケ：罗马音层把英文词当成了日语罗马字（本次）

用户问的：`the pretender up, up, up, up, shake, shake, shake it up, it up`
里**为什么只有 the 没被矫正**。

**先回答 the**：`the`(ザ)、`up`(アップ)、`it`(イット) 都是**离线词典命中** ——
按设计，词典是人工核过的确定答案，排在在线层前面，**不会**送去问模型
（省请求也省钱）。所以"没被矫正"其实是对的，不是漏了。

**但顺手查出了真问题**：`shake` 不是词典命中，是**罗马音层**给的
（`sha-ke` 正好切得干净 → **シャケ**，鮭）。而层序是「词典 > 罗马音 > 大模型」，
罗马音一答，大模型就没机会纠正 —— 这类词**永远读错**。

于是拿 google-10000 英文词频表做了一次审计：9974 个词里 3960 个不在词典，
其中 **240 个**会被罗马音层读成日语罗马字（`open` → オペン、`again` → アガイン、
`joke` → ジョケ、`parade` → パラデ、`poison` → ポイソン …）。两头一起补：

1. **最常见的 90 来个人工钉进词典**（`tools/seed-words.js` 里单独一块，
   含用户这句里的 `shake` → シェイク）。**故意不收**两种读法都成立的那些
   （`sake`/`dare`/`dame`/`ate`/`sage`/`bare`）—— 钉死会毁掉罗马音歌词。
   词典 6077 → **6169 条**。
2. **让在线层来仲裁剩下的**：新增 `core/enwords.js`（英文常用词表，生成物，
   由 `tools/build-enwords.js` 取词频表并剔除词典已有的，约 3700 条 / 26KB）。
   罗马音答案只要落在这张表里就标成 `confident:false`；上层对"没把握"的答案
   按**最低层**算名次，于是排它后面的在线层有资格覆盖它 ——
   `sake` 会被送去问模型、拿到 セイク 就换掉，而真正的日语罗马字
   （`sekai`/`kaze`/`shinjiteru`）不受影响，一个请求都不会多发。
   用户如果把「英文音译规则」拖到在线层前面（纯离线用法），这条名次也跟着变，
   在线层照样不会被打扰。

实测（探针）：一行 `sake sekai the shake pretender` 只问了 `sake` 和 `pretender`
两个词，`sekai` 零请求、`the`/`shake` 词典命中零请求；答案回来后
`sake` → セイク（来源 `lt-src-llm`）、`shake` → シェイク（`lt-src-dict`）。

测试：`reading.test.js` +3（英文词表里的罗马音标成没把握、真罗马音不受影响、
不传词表时行为不变），`integration.test.js` +2（只问该问的词、
纯离线顺序下依然零请求），全套 **226 → 231 全绿**。

### 大模型「请求全失败」：把诊断做扎实（本次）

用户报的：大模型请求怎么全失败了。

**先说我查到了什么**（都做了实测，不是猜）：

- 用本机的 key 直接打服务端：`GET /user/balance` → 200、余额 6.67 元；
  `POST /chat/completions` 和 `/v1/chat/completions` 都 200 并正确返回
  `{"1":"クローバー"}` —— **服务端是好的**。
- 把插件自己在 jsdom 里跑起来（llm.js 全链路 + 真 key + 真 fetch + 真接口）：
  `大模型校正回来 1 个词：命中 1`，页面上 `kaleidoscope` 注成 カレイドスコープ、
  来源类名 `lt-src-llm`，账本记下 421/10 token，`LK.llm.check()` 回「已经生效 ✓」——
  **代码路径也是好的**。

所以问题在"你那一侧的配置或网络"。为了下次能一眼定位，这次把诊断补齐：

1. **key 自动清洗**（这是"全失败"里最常见的一种）：从网页上复制 key 很容易带上
   **引号**、**前后空格**，甚至把整个 `Bearer xxx` 粘进来 —— 这三种都会让请求头不合法
   → 401。现在 `LKLLM.normalizeKey()` 会剥掉它们（引号和 Bearer 叠着也能剥），
   设置面板里**当场洗完写回输入框**，落盘的也是干净值。
2. **错误信息对号入座**：补上 `402 →（余额/额度用完）`、
   `400 →（多半是模型名不对，或这家接口不认 response_format）`；
   对**没有状态码**的失败（`Failed to fetch` 这种对用户毫无信息量的话）
   补一句人话：`（网络不通或被跨域拦住：接口地址要在网易云里能直接访问，
   并且允许 https://music.163.com 这个来源）`。
3. **`LK.llm.check()` 增加「Key 体检」**：长度、形状是否像 `sk-` 开头、
   有没有被清洗过；「请求都没成功」时直接列出 401/402/429/404/400/无状态码各自意味着什么。
   文档里也加了一张对照表（docs/notes.md 的「大模型『请求全失败』怎么查」）。

测试：`tests/llm.test.js` +5（key 清洗四种脏写法、Authorization 真的干净、
402 说清是余额、无状态码失败翻译成人话、stats 里的 key 体检），
`tests/integration.test.js` +1（面板里粘脏 key 会当场洗完并落盘），全套 **220 → 226 全绿**。

### 换歌后那行不注音了：motion 计数器其实是个永久黑名单（本次）

用户报的：换歌之后 `KiLLKiSS judy.., KiLLKiSS jude.., KiLLKiSS juda..,` 这句没注音了。

**复现**（探针：同一个 `<p>` 连续换 5 次文本，每次跑一轮扫描）：第 3 次之后那一行
**再也不标**，而且更糟 —— 整行被跳过，连上一首残留的旧注音都没人去清，
DOM 里新旧注音混在一行（用户看到的"没注音"是框架重建了节点的那种形态）。

**根因**：`motionByHost`（"这个宿主的可见文本一直在变就别追"）的 `changes` 计数
**只加不减**，加到 3 就永久放弃这个元素。而网易云换歌/滚动时**复用同一批
`<li>/<p>`，只换文本** —— 于是同一行被复用超过 3 次以后就永远不注音了。

**修法**：改成**滑动窗口**（默认 3 秒内的变化才计数，窗口一过自动重试），
而且**每次扫描都要判窗口是否过期**，不能只在"文本又变了"时判 —— 否则文本抖几下
安定下来之后就再也没人来重置计数，"某一首之后那一行永远不标"的形态依旧存在。
顺带把这条跳过路径**写进轨迹**（`未注音 文本在动 N 次/3s`）：以前它不留任何痕迹，
所以"某一行没注音"根本查不出原因。真要跟"每帧重建"的插件死磕时，
还有 churn 退避机制兜着（那条是按文字记的，换歌不会继承惩罚）。

**顺带修的第二个坑（同一类问题）**：行首就是拉丁词的行（`KiLLKiSS …` 正是这种），
老版本会把它那个**原文本节点从 DOM 里摘掉**（注音取而代之）。看着更干净，但
框架（React）还攥着那个节点的引用：换歌时它执行 `node.nodeValue = 新歌词`，
说给一个脱链节点听 —— 页面上什么都不变，我们永远看不到"这行换了"，
旧注音留在新歌的行里。现在**保留那个节点**（值清空、注音插在它后面）：
框架写进来的新歌词立刻可见，下一轮就判定失效 → 还原 → 重注。
`kept` 现在两种形态都是 true；行首那种 `head` 存的是**空串**而不是 null
（判据是 `rec.head == null || node.nodeValue === rec.head`，存 null 会变成
"不检查"，反手把框架写进来的新歌词覆盖掉）。

测试：`tests/annotate.test.js` +2（行首拉丁词的行换歌后必须能重注、
快速复用只是暂时跳过且窗口过后自动恢复），并把"注音在行首"那个既有用例改成
按「第一个看得见的东西」断言（行首那个空节点是刻意留的）。全套 **218 → 220 全绿**。

### 「这句不透明度怎么这么低」+ 按来源着色的排障功能（本次）

用户报的：`KiLLKiSS judy.., KiLLKiSS jude.., KiLLKiSS juda..,` 这句的注音特别淡；
顺带要求加一个调试功能：给不同的调用方式标上不同颜色。

**淡的根因有两个，都是 bug：**

1. **失败之后没人重扫，暂定标记一直挂着**。请求还在飞的时候那一轮是暂定的
   （`lt-pending`，样式淡），失败后进入退避、`isWaiting` 变成 false ——
   但大模型那层的失败路径**没有调 `onUpdate()`**（免费接口那边一直有），
   于是注音层永远不知道"不用再等了"。实测：那行能淡**整整一个退避周期**
   （60 秒起，翻倍到 10 分钟），真机歌词一行行滚时看不出来，停在某一句上就很显眼。
   现在失败也通知重扫，那行立刻恢复成确定值。
2. **「注音不透明度」这个设置本身是失效的**。样式里那条
   `rt.lt-rt { opacity: 1 !important }`（本意是挡住别的歌词插件给注音节点打的
   opacity）把用户的设置整个压掉了 —— 于是页面上**只有**"暂定"那 45% 看得见，
   滑块拖了也没反应。现在分开处理：`ruby.lt-ruby`（底字）锁死 1，
   `rt.lt-rt`（注音）用用户的设置，暂定再乘 0.6（跟着设置走，
   不然把不透明度调到 30% 时"暂定"反而比正常还清楚）。

**新功能：按来源着色**（设置面板「外观」→「按读音来源给注音上色」，
或控制台 `LK.colorize(true)`）。每个注音按**最终给出这个读音的层**上色：

| 颜色 | 来源 | 含义 |
| --- | --- | --- |
| 绿 | 离线词典 | 人工核过，最可信 |
| 青 | 记号 / 字母名 | `D/N/A` → ディーエヌエー、`LDK` → エルディーケー |
| 蓝 | 罗马音 | 歌词本来就是罗马字 |
| 橙 | 英文规则 | **拼写猜的**（`hello` → ヘッラオ 那一类），最该怀疑 |
| 紫 | 大模型 | 模型给的 |
| 品红 | 免费接口 | Google 给的 |

实现上：读音查询从"只返回字符串"改成 `{ kana, source }`（`annotate` 两种形状都认，
老约定不受影响），注音节点一直挂着 `lt-src-*` 类，开关只决定要不要注入颜色 CSS ——
所以开关是**瞬时生效**的，不用重扫；`relabel()` 换读音时连来源类一起换
（规则 → 大模型就是这么变的）。

测试：`llm.test.js` +1（失败必须调 onUpdate）、`integration.test.js` +4
（失败后不再淡着、着色类名与颜色 CSS、大模型换色、不透明度真的生效），
全套 **213 → 218 全绿**，`check` 0 警告。

### 加了 API 用量统计（本次）

用户要求：「加个 api 用量统计」。

新增 `core/usage.js`（独立模块）与设置面板的「API 用量」区块，记**本次 / 今天 /
累计**三份账，后两份落 `localStorage['latin-katakana.usage']`（跨天时「今天」自动翻页，
累计不动）：

| 记什么 | 大模型 | 免费接口（Google） |
| --- | --- | --- |
| 请求数 / 成功 / 失败 | ✅ | ✅ |
| 词数 / 字符数 | ✅ | ✅ |
| 输入、输出 token | ✅（接口响应里的 `usage`） | —— 这两个接口不回 usage |

几个刻意的口径：

- **只统计真发出去的请求**：命中缓存不算（省下来的请求在面板里单独显示
  「缓存命中 N 次」）；`fetch` 同步抛异常（请求根本没发出去）只记一次失败、不记请求。
- **花费是估算且默认关着**：面板多了「输入/输出单价」（元/百万 token，默认 0），
  填了才显示金额 —— 各家的价目表随时在变，写死价格等于给用户一个会过期的数字。
- **脏数据一律忽略**：账本只认有限非负数，坏 JSON / 手改坏的都当没有，不影响注音。
- `core/usage.js` 是**可选模块**：没注入时插件照常工作，`LK.usage()` 返回 null。

控制台入口：`LK.usage()` 看账本、`LK.usageReset('session'|'today'|'all')` 清零。

测试：新增 `tests/usage.test.js` 9 例（两层分开记、跨天翻页、三种清零粒度、
坏数据、花费公式、没有 localStorage 的老环境），`correct.test.js` +1（用量回调），
`integration.test.js` +4（token 记账并落盘、面板显示与三个清零按钮、单价算钱、
模块缺失时的降级），全套 **199 → 213 全绿**，`check` 0 警告。

### 读音来源顺序改成用户可调（本次）

用户要求：「让用户自己可调调用顺序」。

**设置面板新增「读音来源顺序」**：五层各一行（离线词典 / 日式罗马音 / 大模型校正 /
免费接口 / 英文音译规则），每行一个 ↑ 一个 ↓，越靠上越优先，另有「恢复默认顺序」。
控制台也留了入口：`LK.layers()` 看，`LK.layers(['rule','dict',...])` 改。

**语义只有一条**：*只有排在「当前答案」前面的在线层，才有资格覆盖它*。所以

- 把**英文规则**提到在线层前面 → 那两个在线层根本不被咨询，**一个请求都不发**
  （"不想联网 / 只看拼写音译"的用法）；
- 把**大模型**提到**词典**前面 → 连词典命中的词也让模型判一遍
  （词典偶有错条目 —— `tick` / `Ave` / `Georgette` 都是用户报过的，这是逃生门）；
- 把**免费接口**提到大模型前面 → 有 key 也先信 Google。

改完顺序会**重扫**（先整篇还原、再按新顺序重注），已注音的词会换来源；
「暂定读音 + `lt-pending` 就地改写」那套底线不变 —— 高优先层还在问的时候不空着。

**实现**：`core/reading.js` 的同步层拆成 dict / romaji / rule 三块可排序
（新增 `setOrder` / `getOrder`，默认仍是 dict > romaji > rule，65 个既有单测一条没改），
异步层（llm / google）的名次在 `main.js` 的 `readForDisplay` / `isProvisional` 里判。
形态层（记号 `D/N/A`、缩写 `I'll`、字母名 `LDK`、长音符罗马字 `Tōkyō`）**不参与排序**：
它们定的是「这个词该怎么断」而不是「读音该信谁」，永远最先判。

**容错**：`layerOrder` 会去重、补齐、丢掉不认识的项 —— 旧版本存的配置没有这个键，
手改坏的（`["rule","rule","乱七八糟"]`）也照样跑，不会少一层。

测试：`tests/reading.test.js` +7（层序互换、脏配置、形态层不受影响），
`tests/integration.test.js` +5（规则提前=零请求、模型压过词典、默认顺序下词典仍然赢、
面板 ↑↓ 改顺序并落盘、坏配置容错），全套 **187 → 199 全绿**，`check` 0 警告。

### 读音准确率：加了大模型两层（本次）

规则层再怎么修也只是**拼写音译**：`hello` → ヘッラオ、`question` → クワエサション ——
那不是人唱的音。所以接了大模型，构建期一层、运行期一层：

**一、构建期：离线词典 491 → 6046 条**（新增 `tools/expand-dict-llm.js`）

- 取英文词频表前 6000，按 40 个一批问 `deepseek-chat`，**只收纯片假名**；
  实测拦下 1 条非法（`portuguese → ポルトガル語` 混进汉字）；
  总花费 8.1 万 tokens、约 4.5 分钟（几分钱）。
- 人工表（`seed-words.js` + `reading.js` 里的例外表/小表共 628 词）**优先**，
  生成物不许覆盖人工判断；产物 `tools/seed-words-llm.js`（5555 条）由
  `npm run build:dict` 合并（新增 `npm run build:dict:llm` 重新生成）。
- **质量交叉验证**：人工核过的 491 条抽 80 条再问一遍模型，**一致率 95%**；
  4 条不一致里 2 条是人工那份更差（`cigarette` タバコ、`complaint` クレーム ——
  那是意译不是音译），另 2 条是两可写法。

**二、运行期：大模型校正层**（新增 `src/core/llm.js`，21 个单测）

- 只对**规则读出来的**词生效：词典与罗马音的结果是确定的，不被覆盖也不发请求。
- 命中缓存立刻用；没命中入队，攒 30 词或 400ms 发一次，结果回来时重扫换掉 ——
  **先显示即时结果，不阻塞注音**。
- 一个词只问一次：命中和"模型也给不出"都永久落 localStorage；
  失败不写缓存、按退避冷却重试（60s 起翻倍到 10 分钟）；限流 20 请求/分钟、20s 超时。
- **只接受纯片假名**（和 Google 那层同一判据）；任何异常都只是"这一层不工作"，
  自动退回免费的 Google 接口。没填 key 时**一个请求都不发**。
- key 只存本机 localStorage，设置面板里填、可点「测试连接」；
  默认 `api.deepseek.com` + `deepseek-chat`，任何 OpenAI 兼容接口都行。
- 真接口联调：16 个词典外的词一批问完 1.5 秒，16/16 命中且全是纯片假名。
- **接口地址自动补全**：DeepSeek / OpenAI 文档里给的是 `base_url`
  （`https://api.deepseek.com` 或 `…/v1`），照抄粘进来 POST 过去就是 **404**（实测）。
  现在会按"少一段补一段"纠正成 `/chat/completions`，设置面板里也会把纠正后的值写回输入框；
  真接口复验：三种写法（主机名 / `…/v1` / 完整地址）全部 200。
  同时把报错信息从 "HTTP 404" 改成"**地址 + 该改哪儿 + 服务端原话**"，
  401/403/429/5xx 各有对应提示。
- 端到端测试（`tests/integration.test.js`）：boot → 规则先给即时读音 → 入队 →
  一批问完 → `onUpdate` 重扫 → DOM 里的注音被换成大模型的写法。

**三、顺手补的两处**

- `tests/dict.test.js`（新增 5 个用例）：词典的数据纪律 + 一条不变量 ——
  规则层对**词典里每个词**都必须吐出纯片假名。这条当场抓出规则层的调试残留
  （`@@`、`undefined`），修完才让它常驻。
- 规则层的 r 化元音「段」修正：`ur/ir/er` 配 ア段、`or` 配 オ段、`wor-` 配 ア段
  （修正前 `turn → トゥーン`、`bird → ビード`、`river → リベー`；现在
  ターン/バード/リバー，`fork → フォーク`、`form → フォーム` 也跟着修好了）。

### 预览图改成 750×350（和 jp-furigana 同尺寸）（本次）

- `src/preview.png` 换成用户自己裁的歌词区截图（《快晴》那句
  「I know this sky loves you.」+ アイ ノウ ディス スカイ ラブズ ユー + 中文翻译），
  尺寸按 [jp-furigana](https://github.com/Leleawa/jp-furigana) 的 `preview.jpg`
  对齐成 **750×350**（2.143:1），商店里两个插件并排时构图一致。
- 做法：拿 424×237 那张小图**一次**高质量双三次放大到 750 宽（→750×419），
  再把底部背景**拉伸一行**补 24 px，然后贴底裁 750×350 —— 这样上边是干净的背景，
  切掉的全是那行**淡出的上一句译文**，而下面「いずれ全て / 就算总有一天」整行保留。
  直接按 750×350 居中裁会把上下两句各切一半。
- 包体 1.31 MB → **0.30 MB**（新预览图 170 KB，比原来那张 1585×1128 的全窗口截图小 7 倍）。

### 设置面板预览换成高考听力那句；顺带补上数字词（本次）

**一、预览示例句**

设置面板「预览」区块的示例句，从「きらめく light と clover、それから Sekai へ。」
换成高考英语听力那句名句：原文 `The shirt is nine pounds fifteen pence.`
（标成 ザ シャツ イズ ナイン パウンズ フィフティーン ペンス），
下面跟一行中文翻译「衬衫的价格为九磅十五便士」—— 翻译行**刻意不注音**，
跟真机行为一致（默认歌词页的翻译层是跳过的），免得给人错的预期。

**二、`fifteen` 显示成 フィファテエン，牵出一整族数字词的缺口**

改预览时用真读音路径探了一遍示例句，发现 `fifteen` **不在词典里**，
走规则层给的是 フィファテエン。顺着查了一整族，漏的不止一个（都是**人工钉死**）：

| 词 | 规则层/罗马音层给的 | 词典里定死 |
| --- | --- | --- |
| thirteen | シアーテエン | サーティーン |
| fourteen | フォアーテエン | フォーティーン |
| fifteen | フィファテエン | フィフティーン |
| sixteen / seventeen / eighteen | シクステエン / セベンテエン / エアイテエン | シックスティーン / セブンティーン / エイティーン |
| nineteen | ニネテエン（被罗马音层抢走） | ナインティーン |
| forty / fifty | フォータイー / フィファタイー | フォーティ / フィフティ |
| sixty…ninety | シクスタイー…ニネタイー | シックスティ…ナインティ |

根因：这批词**不在那份词频表的前 6000 里**（表里只有 one/three/nine/twenty/thirty
这些），所以从来没进过生成词表。顺手把 `two` / `seven` / `eight` / `eleven` /
`second` / `hour` / `pence` / `penny` 一并定死（它们走规则虽然读对了，但没权威来源）。
词典 6056 → **6077 条**（人工 506 → 527）。

**三、README 加图 + 两处过时说明**

- 加上全屏歌词页的真机截图（`docs/images/lyrics-page.png`，不进插件包）；
- 「在线那层还没回来时**先不标**」是上一版的行为，早改成暂定读音 + `lt-pending` 了，照实改；
- 词典条数从 6057 改成 6077，并把「制作信息行 / 默认页翻译层跳过」写进去。

回归测试：`tests/integration.test.js` 新增一例钉住预览的七个读音 + 翻译行不注音；
全套 **186 → 187 全绿**。

### 预览图换成真机截图（本次）

- `src/preview.png` 改成用户提供的真机截图（网易云歌词页，《快晴》那句
  「I know this sky loves you.」上面标着 アイ ノウ ディス スカイ ラブズ ユー）——
  原来那张是 `tools/make-preview.js` 画出来的示意图（480×240、2 KB），
  示意图看不出插件在真机上的样子，README 头图也一直不好看。
- 顺手删掉那个生成器（含 `npm run build:preview`）：它的输出就是覆盖
  `src/preview.png`，留着只会在哪天误跑一次、把真机截图换成占位图。
  截图本身进了插件包（`manifest.json` 的 `preview`），包体 0.14 MB → 1.34 MB，
  相对插件包本身可以忽略。

### 网易云默认歌词页：翻译层和「编曲」行不再被注音（本次）

用户报的：默认歌词页里的**翻译**和**编曲**也被注上了片假名。

定位（都有真机证据，不是猜的）：

1. 默认页的结构是 `ul#mod_pc_lyric_record.lyric > li.line > p × 2`
   （第一个 `<p>` 原文、第二个是**中文翻译**）—— 这条来自 jp-furigana 源码里标着
   「NCM 3.1.36 实测」的注释，跟我们自己轨迹里的「默认页的クローバー」对得上。
   而我们的歌词选择器把 `ul.lyric > li` 排在**最前**，于是整个 `<li>` 成了区域，
   翻译那一块是它的子节点，跟着原文一起被注了音。
2. 制作信息正则里只有 `作[词詞曲編编]`（作词/作曲）——**`编曲` 从来不在名单里**，
   所以「编曲 : Kenji」的 Kenji 照标；标签和名字分属两个 `<p>` 时，
   名字那一块连"行首关键字"都看不到，更拦不住。

改法：

- 歌词选择器改成**越细的行排越前**：先 `ul.lyric > li > p`，再 `ul.lyric > li`。
  收集区域时新增两条**歌词专属**规则（只对歌词那组选择器生效；播放栏那组是
  「歌名 + 歌手」结构，套用会把歌手丢掉）：
  - **同一个 `<li>` 里只留一块**，判据用**假名**而不是 class（那两层通常没有可用的
    class）：日文原文必有假名、中文翻译一定没有。已经有带假名的一块就扔掉后来者；
    前一块没假名而这一块有，说明前一块是翻译层，换掉它；两块都没假名
    （纯英文原文 + 中文翻译）时留**第一个** —— 网易云原文在前。
  - **不再收已经被更细的行覆盖的祖先**，否则刚踢掉的翻译层会跟着父区域 `<li>` 回来。
- 制作信息正则补齐中文词（编曲 / 編曲 / 母帶 / 製作人 / 出品 / 监制 / 吉他 / 贝斯 /
  键盘 / 弦乐 / 和声…），中文那一支要求后面跟冒号；英文那一支要求跟 `:` / `by` / `-`，
  免得 `Music と light の 中` 这种歌词行被误杀（反向用例也钉在测试里）。
- 顺手把 RNP 3.0.2 bundle 里的实际 class 全捞出来核对，跳过名单补上
  `rnp-lyrics-overview-line-romaji / -translation / -placeholder`（总览页那两种变体
  老正则漏了），并加了通用兜底 `trans / translated / translation / romaji`
  和 AMLL 那类 `lyricSubLine`；同时把 `div[class^="rnp-lyrics-overview-line"]`
  加进歌词选择器 —— 总览页的原文行以前根本没被标。

回归测试：默认页三种 fixtures（原文 + 翻译、纯英文原文 + 翻译、空占位块 + 原文）、
制作信息四种写法、标签与名字分块的编曲行、RNP 总览页三层、以及
`Music と light の 中` 的反向用例 —— 新增 7 个用例，全套 **179 → 186 全绿**。

### 全英文行"标注后消失"：等待在线结果时改用暂定读音（本次）

用户报的：全是英文的一行，标注之后**有概率消失**。

定位到三个机制（都跟上一版"在线优先、规则垫底"的实现方式有关）：

1. **等待期间"先不标"会把整行拖空**：一行里只要有一个词典外的词在等在线结果，
   这行就注不上；而这个词所在的**原文本节点已经有记录了**，等结果回来时这一轮扫描
   会跳过它（记录完好），于是**一行的首个词**永远补不上 ——
   真机复现：`kaleidoscope zephyr serendipity light` 里 `kaleidoscope` 一直是裸文本。
2. **在线结果回来时走 restoreAll + 重注**：等于把整行先清空再补回来，
   清空那一刻如果还有词在等，看到的就是"消失"。
3. `applyBatch` 只在"有命中"时才通知上层重扫 —— 一批全是"给不出"时没人重扫，
   那些词会一直空着（轨迹里实测空了 **2.3 秒**）。

改法：

- 等待在线结果时**不再空着**：用规则读音当**暂定值**，注音加 `lt-pending` 类
  （样式淡到 45%）提示"还不一定"；拿到结果由新增的 `annotate.relabel()`
  **就地改写**（DOM 节点一个都不动，连重建都没有）并去掉标记。
- 在线结果回来时不再 `restoreAll`，改成 `relabel()` + 一次扫描补新词。
- 两层（大模型 / 免费接口）**不管有没有命中都通知重扫** —— miss 也是状态变化，
  规则层该接手了。
- 语境改用**宿主的可见原文**（注音前后不变），不再用文本节点自己的值：
  后者在注音之后会被我们切短，同一个词会算出两个不同的缓存键、被反复问。

回归测试：全英文行的暂定标注 → 拿到结果就地改写（断言**同一个节点对象**、没重建）
→ 模型给不出的词保持规则读音 → 同一个词不重复问。

### Georgette 注成 ゲオーゲターテ：专有名词进人工词表（本次）

用户报的：`Georgette` → ゲオーゲターテ。（顺手确认：输出是**片假名**、不是平假名 ——
`reading.js` 的表里没有平假名混入，我扫过。）

原因很直白：这个词不在词典里，走的是英文拼写规则，而 `geo` / `rge` 这种拼法规则只能瞎猜
（英文的软 g / 硬 g 光看拼写定不下来：`get` 是硬 g、`gem` 是软 g）。
专有名词只能人工核 —— 写进人工词表定死 **ジョージェット**（织物义另有 ジョーゼット）。

**顺带修了首音校验的两个会错杀正确答案的洞**（上一版加的）：

- `think` シンク（th → サ行）、`phone` フォン（ph → ファ行）—— `t` / `p` 开头的白名单里
  原来只有 タ行 / パ行，会把 θ / φ 的正确音译判成"不是音译"直接丢掉；
- `gem` ジェム、`George` ジョージ、`giant` ジャイアント（软 g → ジ行）、
  `chat` チャット（ch → チ）—— `g` / `c` 的白名单里原来没有软音那一档。

现在同一个字母的多行都放行，测试里把这 7 个都钉住了（配上原有的"拦拟声词"用例）。

### Ave Mujica 注成 アベニュー：缩写被展开（本次）

用户报的：`Ave Mujica` → **アベニュー ムジカ**。

定位：词典里 `ave` 那条就是 **アベニュー** —— 大模型把 Ave 当成了地址里的 "Ave."
（avenue 的缩写）展开。`Mujica` 反而是对的（ムジカ，走罗马音）。

- **把 `Ave` 在人工词表里定死为 アヴェ**：它是拉丁语的"万福"，
  Ave Maria = アヴェ・マリア 是通行写法。人工词表优先于生成词表，以后不会再被覆盖。
- 顺手把同一类"缩写被展开"的生成词条列进黑名单（新增 `tools/dict-blocklist.js`，
  构建与测试共用一份，避免"加一条黑名单就把不变量测试搞红"）：
  `avg` → アベレージ、`blvd` → ブールバード。
- 把词典里 **185 条"词短、读音长"的条目全人工过了一遍**：绝大多数是正确的字母名读法
  （abc エービーシー、cpu シーピーユー、dna ディーエヌエー、ceo シーイーオー…），
  错的只有上面这类"缩写展开"。
- 词典 6057 → **6055 条**（人工 505 + 生成 5550）。

### tick 注成 カチカチ：在线答案加首音校验（本次）

用户报的：`tick` 被注成 **カチカチ**。

先定位清楚：**规则层给的是对的**（`tick` → ティック），问题在**在线那层** ——
Google 的 en→ja 对 tick 会回「カチカチ」这种**拟声词**（"滴答声"），
而且它是纯片假名；我们原来的唯一判据"只收纯片假名"**拦不住意译**
（同理模型也可能回「ダニ」＝蜱虫这种名词义）。

三处改动：

- **首音校验**（`reading.js` 的 `looksLikeTransliteration()`，由 main.js 注入给两层）：
  词首是稳定辅音时，音译的首音必然落在对应的行上（t 只能出 タ行、k 只能出 カ行…）。
  `tick` + カチカチ 的首音是 カ → 判为"这不是音译"，丢掉并记成 miss（不再重问）。
  刻意保守：h / w / y / 元音开头，以及 kn / gn / ps 这类带默字的组合**一律不校验** ——
  `write` → ライト、`hour` → アワー、`knife` → ナイフ、`psychology` → サイコロジー 都照收。
- **提示词加一条**：明确"这是音译不是翻译，不要给日语词、不要给拟声词"
  （`tick` → ティック 而不是「カチカチ」、`beat` → ビート 而不是「鼓動」）。
- 顺手把 `tick` / `tock` 收进人工词表（时计声在歌词里常见，词典命中就轮不到在线层）。
  **后来按用户的选择撤掉了**：`tick` 该写 ティック 还是 チック 是个口径问题
  （ティ = [ti] 更忠实、和词典里的 `stick` スティック 一致；チ = [tɕi]「chi」是英和辞典/大模型的老写法），
  所以不进词典，交给大模型按语境决定 —— 拟声词/意译那条防线（首音校验）照旧拦着。

### 缩写 's / 're / 'll / 'd / 've / 'm / n't（本次）

用户报的：`you're` / `I'll` / `it's` / `I'd` 这类注不准。
（`vs.` → バーサス 其实上一版已经修好了 —— 是 `vs` 那条人工词条，重启后生效。）

根因是折撇号那一步：`normalize()` 会把 `'` 去掉，于是

- `I'll` → `ill`（词典里真有 ill = イル）
- `I'd` → `id`（词典里真有 id = アイディー）
- `you're` → `youre`（谁也不认识 → 规则猜成 ヨウレ）

所以缩写必须**排在词典前面**：拆成"词干 + 缩写尾巴"，词干递归走正常路径
（词典 / 罗马音 / 规则），尾巴按音变补上：

| 尾巴 | 读法 | 音变规则 |
| --- | --- | --- |
| `'s` | ズ | 词干以 ト 结尾并成 ツ（イット → イッツ）、以 ド 结尾并成 ズ（キッド → キッズ） |
| `'re` | ア | 词干的长音收掉（ユー → ユ + ア = ユア） |
| `'ll` | ル | I'll → アイル、you'll → ユール |
| `'d` | ド | I'd → アイド |
| `'ve` | ブ | I've → アイブ |
| `'m` | ム | I'm → アイム |
| `n't` | ント | don't → ドント |

词干是规则猜的，来源就记 `rule`（大模型那层照样会接手）；命中词典就记 `dict`。
整词表兜住拼不出来的：`don't` ドント、`can't` キャント（ca 在词典里是字母名 シーエー）、
`won't`、`ain't`、`weren't`、`couldn't` / `wouldn't` / `shouldn't` / `mustn't` /
`hadn't` / `needn't`、`they're` ゼア、`we'll` ウィル、`y'all` ヨール。

### 读音顺序改成「大模型 / 免费接口 > 英文音译规则」，并且带上语境（本次）

用户提的两条，一起做了：

**一、让大模型联系上下文**

- 请求里除了词，还带上它所在的**整句歌词**（条目形状 `[{i, w, line}]`，响应按 `i` 对齐，
  所以同一个词在两句里不会互相覆盖）。提示词里明确要求"同一个词在不同句子里读音不同时
  分别判断"（`read` 在 “read a book” 里 リード、在 “I read it yesterday” 里 レッド）。
- 缓存键从"词"改成"**词 + 那句歌词**"：同一句里的同一个词仍然只问一次，
  换一句才再问一次。只按词缓存的话，先遇到哪句就把哪个读音钉死一辈子。
- 语境折叠空白、截断到 160 字符（不会把整本歌词塞进提示词）。
- 控制台 `LK.display('词')` 没有句子可给，于是另存一份"词 -> 最近一次读音"的索引
  （`peek()`）给它用；**页面注音仍然严格按当前这句的语境取**，不会串味。

**二、层序反过来：在线优先，规则垫底**

以前的顺序是「词典 -> 罗马音 -> **规则** -> 大模型 -> 免费接口」，规则猜出来的读音会先显示，
等在线结果回来再换掉。现在是：

| 顺序 | 来源 | 行为 |
| --- | --- | --- |
| 1 | 词典 / 字母名 / 记号 | 确定，直接用 |
| 2 | 罗马音 | 确定，直接用 |
| 3 | **大模型**（配了 key） | 优先；等待期间**先不标**，回来再补 |
| 4 | **免费接口**（没配大模型时） | 同上 |
| 5 | 英文音译规则 | 只有 3/4 给不出、失败、或没配时才用 |

代价是词典外的词会晚半秒到一秒才出现（先不标一个错的，好过标错再改），
所以加了一条硬底线：**在线那层挂了/在退避/没配 key 时，`isWaiting()` 立刻返回 false，
规则立刻兜底** —— 断网不会变成一个词都不标。为此还修了免费那层一个小问题：
请求失败时只记 miss、不通知上层重扫，于是"先不标"的那一轮永远补不回来；
现在失败路径也会 `onUpdate()`。

**三、顺手抓到一个真 bug**：`applyBatch` 里有个局部变量 `byWord` 把外层同名的
"词 -> 读音"索引**遮住了**，于是 `peek()` 永远是空、控制台看不到大模型的结果。
是新加的端到端断言把它逼出来的（测试先红，再定位，再修）。

回归测试：`isWaiting` 的四种状态（等待/有结论/miss/退避）、
同一词不同语境分别问分别记、同一句只问一次、语境截断、
"等待期间不标、失败后回落"、"完全离线立刻用规则"。

### 缩写：Mr. / Dr. 念整个词，LDK / TV 这类逐字母（本次）

用户报的：`Mr.`、`Dr.`、`LDK` 等注错了。改之前是规则层硬拼的结果：
`Mr` → **ンー**、`Dr` → **ドー**、`Mrs` → **ンラス**、`LDK` → **ラダク**、
`TV` → **タブ**、`BGM` → **バグム**、`RPG` → **ラパグ**、`DVD` → **ダバド**。

- **缩写进人工词表**（`tools/seed-words.js`）：`mr` ミスター、`mrs` ミセス、`ms` ミズ、
  `dr` ドクター、`prof` プロフェッサー、`jr` ジュニア、`sr` シニア、`vs` バーサス、
  `etc` エトセトラ、`ltd` リミテッド、`inc` インク —— 日语里念的是整个词，不是字母名。
  有歧义的没收（`St.` セント/ストリート、`No.` ナンバー/ノー）。
- **全大写无元音缩写逐字母读**（新的 `spellOutAcronym()`）：`LDK` エルディーケー、
  `NHK` エヌエイチケー、`CD` シーディー、`TV` ティーブイ、`BGM` ビージーエム、
  `RPG` アールピージー、`DVD` ディーブイディー。判据刻意收得紧，免得误伤真词：
  原文**全大写** + 长度 2~6 + 一个元音都没有，而且 **y 也算元音**
  （否则 my / sky / why / fly 会被逐字母念）；小写感叹词（hmm / tsk / shh）
  和混大小写（Ldk）都不受影响。
- 词典里已有的缩写仍然走词典（`CM` シーエム、`DJ` ディージェイ），互不冲突。

### 带变音符号的拉丁字母（Ō / é / ü …）（本次）

用户报的：`Ō` 等没注音上。根因是 matcher 的字符集只有 ASCII `[A-Za-z]` ——
后果不只是漏标：`Tōkyō` 会被切成 `T` + `ky`，而 `ky` 单独命中词典读成 **ケーワイ**，
比不标还糟。

- matcher 的字符集扩到 Latin-1 Supplement / Latin Extended-A、B / Latin Extended
  Additional，扫描循环的**首字符判断**也跟着改（只改正则不改这里的话，
  `Ōkami` 会在 Ō 上直接跳过，只剩 `kami` 被当成一个词）。
- 读音层新增 `foldLatin()`，分两组处理：
  - **长音符 ā ē ī ō ū**（日语罗马字的写法）折成「元音 + `-`」，而本仓库的罗马音
    口径里 `-` 就是长音 → `Tōkyō` **トーキョー**、`kōhī` **コーヒー**、
    `arigatō` **アリガトー**、`Ō` **オー**。这一组排在词典**前面**
    （长音符是日语罗马字的标志，按罗马音读更贴近唱出来的音）。
  - 别的变音符号折成基础字母（`Café` → cafe、`déjà` → deja）：词典优先
    （`Café` **カフェ**、`José` **ホセ**），词典没有的标 `confident: false`
    交给大模型/联网（读音取决于语种，José 是 ホセ 不是 ジョセ）。
- 顺手挡掉一个折叠后才露出来的坑：折叠写法**不能**再走"去掉非字母"那一档词典键，
  否则 `déjà` 会被削成 `dj`，正好命中词典里的 DJ → **ディージェイ**。
- 大模型层的缓存键也折变音符号（`Tōkyō` → `tokyo`）：原来 `/[^a-z]/` 会把它削成
  `tky`，缓存键对不上，还会拿半个词去问模型。
- 测试：matcher 字符集与单字母规则、折叠表、长音符/其它变音符的读音与优先级、
  大模型键、以及一整行的端到端（`Tōkyōの夜を arigatō と歌う`）。

### 诊断：怎么看大模型有没有生效（本次）

用户问「如何判断大模型是否生效」。顺手补了三处，并修掉一个**文档骗人**的问题：

- **`LK` 短别名根本没挂**：启动日志、README、排障文档写的都是 `LK.stats()`，
  但代码里只挂了 `window.LatinKatakana` —— 照着敲会 `LK is not defined`。
  现在两个名字都挂（同一个对象）。
- **`LK.llm.check()`**：把 stats 的数字翻译成人话 + 下一步该做什么。
  重点是回答最容易误会的那一条：`请求 0 次` 既可能是"没填 key"，
  也可能是"歌词里的词全在词典里、这层没活干" —— 现在会分开说。
- **`LK.display(word)`**：页面上**实际用的**读音（可能被大模型换过），
  和 `LK.read(word)`（本地那几层）对照就能看出某个词是谁给的。
  配合 `LK.dict()['词']` 先确认它在不在词典里（词典命中是终点，不会走大模型）。
- 文档里补了一节「怎么判断大模型有没有生效」（四级判据 + 常见误会），
  并把轨迹里的 `[llm]` 行也列进排障清单。
- 测试：`tests/integration.test.js` 新增两条（别名/check 的四种结论、
  display 与 read 的差异），顺手让测试桩把免费那层的 GET 请求也拒掉，去掉噪音。

### 记号逐字母读（本次）

用户要的：`D/N/A` 这种记号按字母名念出来（**ディーエヌエー**）—— 既不漏标，
也不能把里面的 A 当英文冠词读成 ア。

- `core/latin.js` 先认**记号**：字母被分隔符一个个串起来的整体算**一个词**
  （`D/N/A`、`N/A`、`A.B.C`、`R&B`、`X-Y`）。判据是"每一段都是单个字母"，
  所以 `e-mail` / `x-ray` 仍是普通词，不会被逐字母念。
- `core/reading.js` 新增字母名表与 `notationToKatakana()`，读音来源记作 `letters`
  （reader 的 stats 多一项 `letterHits`）；分隔符不发音，`&` 读 アンド
  （`R&B` → アールアンドビー）。
- **记号必须排在词典前面**：`dna` / `abc` / `na` 这些词条就在词典里，
  晚一步就会读成单词音（`N/A` → ナ）。测试里专门钉住这条。
- 注音层和设置面板的预览改成把**原始写法**交给读音层 —— 折过的形式会把连字符吃掉，
  `X-Y`（记号）和 `x-ray`（词）就分不出来了。
- 字母名用通行读法：G ジー、H エイチ（不是 エッチ）、Z ゼット。

### 记号里的单字母不注音（本次）

用户报的：`だって D/N/Aじゃ 騙れない` 里那个 **A 被注成了 ア**。
上一版刚把 `a` / `I` 放行，于是 `D/N/A` 被切成 D、N、A 三个单字母记号，
其中 A 命中了白名单、按英文冠词读成了 ア —— 但它其实是标题记号的零件。

现在 `scan()` 会给单字母打一个 `glued` 标记：**紧挨着的前后一个字符是分隔符**
（`/ \ | _ . -`、全角斜杠、中点，以及 `& # * ~ + =` 这类装饰符号）时，
它算"粘在记号上"，不标。验证：

| 输入 | 结果 |
| --- | --- |
| `だって D/N/Aじゃ 騙れない` | 一个字母都不注 ✓ |
| `N/A` / `A.B.C` / `X-Y` / `&A&` | 不注 ✓ |
| `A story` / `Tell me a story` | A → ア ✓（句首冠词照标） |
| `I love you` | I → アイ ✓ |

代价：`A.` 这种"字母紧跟句号"的写法不再注音（比把 `A.B.C` 的 A 注成 ア 划算得多）。

### 换歌：上一首的歌词不会再留在下一首里（本次）

用户报的：「下一首歌会出现上一首歌的歌词」。网易云的歌词列表会**复用同一批节点**，
换歌时只把文本节点的值换成新歌词 —— 而我们的记录里还攥着上一首的原文，两个坑同时踩了：

1. **跳过条件太粗**：那一轮为了修「RNP 把我们的 `<ruby>` 抹掉、底字没变」，
   把判断写成了"注音还在就跳过"。注释里其实写着「底字没变 ≠ 注音还在」，
   但反过来"注音还在 ≠ 这行还是我们的"：换歌之后我们的 ruby 还挂在原地，
   于是每轮都跳过，**上一首的注音永远留在新歌的行里**。
   现在两条一起看：`annotationsIntact(rec) && !isStale(rec, node)`。
2. **还原会写回旧文字**：`restoreRecord()` 无条件 `node.nodeValue = rec.plain`，
   换歌后那个节点的值已经是新歌词了，写回去就是把**上一首的歌词搬进新歌的行里**，
   而且框架认为这行没变、不会自己修回来。现在先确认"这行还是我们注的那句话"
   （`kept=true` 看原节点的值是否仍是我们写下的那截前缀；`kept=false` 看注音还在不在
   且宿主可见原文没被换过），不满足就**只摘掉我们插的节点，DOM 里的文字一个字都不改**。

顺带修掉一个被上面那道粗闸掩盖的老问题：`isStale()` 拿**整个宿主的可见文本**去和
**单个文本节点的原文**比，宿主里只要有别人的注音底字（`kt-ruby` 里的 コーヒー 这种）
就永远不相等 —— 结果每轮都判"馊了"→ 还原 → 重注。现在记录里存一份
"注入当时宿主长什么样"（`hostPlain`）按它比，`tests/annotate.test.js` 里那条
"别人的注音不算底字"的用例正是靠它才成立的。

回归测试（两条，都是照着用户的现象写的）：换歌后新歌词必须原样留着、旧注音撤干净、
新歌里的词要能重新注上、而且不能被当成打架而认输。

### 读数准确率：单字母词 `a` / `I` 也标（本次）

用户报的：`Tell me a story tell me a story 叶うなら` 这一行里，其它词都注上了，
只有 `a` 空着 —— 那是早期"单字母一律不标"的规则干的（当初是为了躲开首字母缩写
和排版噪声）。可 `a` 和 `I` 是**真正的英文单词**，在 J-pop 歌词里满地都是，
漏掉它们比标错还显眼。现在：

- `looksReadable()` 放行 `a` / `I`（`I` 归一化后是 `i`），其它单字母照旧不标；
- 词典里补上这两条（`a` → ア、`I` → アイ，人工词表 493 条）；
- 端到端用例把用户那一行原样钉住：`Tell テル / me ミー / a ア / story ストーリー`；
- 已知取舍：罗马音歌词里孤零零一个 `i`（= い）会被读成 アイ —— 但 RNP 的罗马音层
  本来就整层跳过，纯罗马音行里的裸 `i` 极少，而英文部分的 "I" 远比它常见。

### 规则层重写：按 sljfaq 的约定

英文音译规则（`src/core/reading.js` 的 `convertEnglish`）改成按
sci.lang.japan FAQ 的
[How do I write an English word in Japanese?](https://www.sljfaq.org/afaq/english-in-japanese.html)
实现，代码里每条规则/每张表都注了 `参照 sljfaq：…`。改动要点：

- **补齐了参考页点名的规则**：非重读 r（car/bird/horse/park）、
  θ/ð（think/the/-ther）、`ti/di` → ティ/ディ、`dz` → ッズ、
  `æ after k` → キャ、辅音 + 词尾 y → イー、`-Cle` → クル/プル/…、
  m/n 在辅音前收 ン、单音节词词尾塞音促音、`-ture` → チャー、`-ous` → アス。
- **v 从 ヴァ行改回 バ行**（参照 sljfaq：バ行是首选写法，love → ラブ、
  vitamin → ビタミン）；ヴィ 只在日语实际那么写的词里保留（visual）。
- **促音保守化**：参照页面的说明「多音节词只在重读音节促音」，
  而重音光看拼写定不下来，所以只对单音节词的词尾塞音补 ッ，
  多音节词交给 `confident: false`，不猜。
- **删掉了「声门塞音式」的旧表**（`EN_UNSURE` 里的 th/wh/gh 一律标不放心）：
  改成按拼写歧义清单（`EN_UNSURE_RE`）判断，
  θ/ð 的**读音**照页面给（シ / ザ），只有「哪个词是 θ」拼不出时才标不放心。
- **硬契约加严**：`englishToKatakana()` 的输出保证是纯片假名（ァ-ヶ + ー），
  非法字符统一清理并置 `confident: false`；由 `tests/dict.test.js`
  的不变量测试遍历全部词典词守住。
- **小表/例外表扩充**：新增 phone / sugar / visual / violin / vitamin /
  tourette / moon / moonlight / sunshine / destiny / goodbye / remember /
  together / second / button / cotton / common / hamburger 等
  规则拿不准但歌词高频的词。

**验证**：`npm run test:serial` 全部通过（latin 11 / reading 54 /
sljfaq-words 3 / annotate 9 / correct 10 / integration 13 / patch 8），
`tools/check.js` 通过。新增 `tests/sljfaq-words.test.js`：
把参考页的例子分成「读得对」与「读不出来（逐条附理由）」两张表，
页面上出现的例子必须登记，不许静默跳过。

**已知缺口**（拼写决定不了，页面自己也说先查词典）：
`ow`（now/snow）、`oo`（book/moon）、`oo+r`（door/four）、`our`（hour/tour）、
`θ/ð` 同形、`-er` 的两个读音、`igh` 被 `f` 拼块抢先（fight）、
专有名词（Disney/Washington）、词典形（pajamas/slippers/router/tarot）。
完整清单与理由见 `tests/sljfaq-words.test.js` 的 `KNOWN_GAPS`。

### 读音从哪来（四层）

| 顺序 | 来源 | 例子 | 可信度 |
| --- | --- | --- | --- |
| 1 | 离线词典（491 条） | `clover` → クローバー、`light` → ライト | 确定对 |
| 2 | 罗马音切分 | `sekai` → セカイ、`shinjiteru` → シンジテル | 确定对 |
| 3 | 英文音译规则 | 兜底，永远给得出结果 | 猜的（标记"没把握"） |
| 4 | 联网校正 | Google en→ja，**只接受纯片假名** | 更准 |

词典的 491 条来自两处：把片假名终结者的离线词典反转（那份本来就是
"片假名外来语 → 英文原词"），加上手工补的歌词高频词与**变形词**。

补变形词是实测逼出来的：规则吃的是拼写、不懂英文音变，所以
`shining` → シイニング、`dancing` → ダナシング、`stories` → サトリエス、
`running` → ルッナイング 全是错的，而 J-pop 歌词里恰恰全是这种词。
现在它们直接进词典。

### 标注范围

- 歌词原文行里的拉丁词 ✅（**含汉字的行也照标** —— 拉丁字母跟振假名不是同一批字，
  没有让位的理由；这是和片假名终结者最大的语义差别）
- 播放栏的歌名 / 歌手 ✅
- RNP 的**罗马音层**和**中文翻译层** ❌（本身已经是读音或译文）
- 单字母、制作信息行 ❌

### 与另外两个插件共存

DOM 注入、还原、churn 认输、`age` 闸门、同步修复钩子这套机制是从
片假名终结者那边继承的（都是真机踩出来的），另外做了四件事：

1. 共存补丁的识别范围扩到**两家**：`kt-ruby/kt-rt` 与 `lt-ruby/lt-rt`，
   标记位 `__ktOwned` / `__ltOwned` 都认。少认一家的后果不是"少标一点"，
   而是那一家的注音会让行被判脏并重建 —— 只有它一家闪，极难查。
2. 修复钩子**链上去而不是覆盖**：`window.__ktRepairLine` 可能已经被
   片假名终结者挂了，直接赋值会把它顶掉，那边立刻开始闪。
3. 片假名终结者也加了互认（2.1.1）：它的"可见原文"现在排除三家注音
   （`kt-rt` / `lt-rt` / `fg-rt`），否则内核不支持 ruby、我们降级成
   `<span class="lt-rt">` 时，它会把我们的片假名当成"底字变了"。
4. 反过来我们也认它们：`isSkippable()` 会整棵跳过 `kt-ruby/kt-rt` 与
   `fg-ruby/fg-rt`。片假名终结者的注音里装的偏偏是**英文原词**
   （`<rt class="kt-rt">dream</rt>`），正是我们要标的对象 —— 不跳就会
   在英文注释上面再注一层片假名，等于给注解做注解。
   这条同样只在"内核不支持 ruby、两家都降级成 `<span>`"时才需要 class 兜底，
   所以 `tests/integration.test.js` 里两条路径各钉了一个用例。
5. 片假名终结者的同步修复钩子也改成**链式**（2.1.2）：它原先直接覆盖
   `window.__ktRepairLine`，谁后加载谁说了算。如果它加载在我们后面，
   我们就被顶掉、收不到同步通知（于是只剩 MutationObserver 那条"晚一帧"的路）。
   现在两个方向都能链，加载顺序不影响功能。

### 工具链

- `tools/make-preview.js`：零依赖手写 PNG 编码器，生成 `src/preview.png`
  （`npm run build:preview`）。这张图之前是从隔壁「片假名终结者」误拷过来的，
  内容对不上，现在是本插件自己的示意图。
- `tests/patch.test.js`：把共存补丁的自检搬进本仓库，并补上本项目独有的回归点 ——
  补丁必须**同时认出两家**插件（`kt-ruby/kt-rt/kt-ov-label` 与
  `lt-ruby/lt-rt/lt-ov-label`），`__ktTextIsOurs` 必须同时接受 `__ktOwned`
  和 `__ltOwned`。这两个断言是真的把补丁写进去的 helper 切出来执行，不是字符串比对。
- `tools/check.js` 的测试文件清单加上新文件，并且同时校验 `test` 与 `test:serial`
  两个脚本都登记了它（沙箱里唯一跑得通的是 `test:serial`，漏登记会完全看不见）。
- `tools/verify-install.js`（`npm run verify:install`）：把装到 `C:\betterncm\plugins`
  的那个包解开，逐个文件和仓库 `src/` 做**逐字节**比对，顺带核对版本号、
  注入清单与两个共存关键串。`.plugin` 是复制过去的，"改了代码但真机没变化"
  这种坑不用再靠猜。

### 验证

- 单元测试 **136 个全过**：拉丁词扫描 11 / 读音引擎 54 / sljfaq 例子 3 /
  词典纪律 5 / DOM 注音 9 / 联网校正 10 / 大模型层 21 / 集成 15 / 共存补丁 8
- `npm run check` 通过（0 警告，9 个测试文件全部登记）；读音词典 **6046 条**，
  键是英文、值是纯片假名
- 规则层不变量：词典里全部 6046 个词过一遍规则，输出必须全是纯片假名
- CI 覆盖：静态自检 + 全部测试 + 打包 + 打 tag 发 Release

### 已知限制

- 规则音译只是兜底：`hello` → ヘッラオ 这种还不准；词典覆盖了词频前 6000，
  其余交给大模型（配了 key）或免费接口
- 规则层有几处比老版本更差的拼写（`my` → マイー、词尾 `-se`、二合字母 + r 等），
  这些词全在词典里，真机走不到；清单见 README「已知限制」
- 人工词表里有 2/80 条是"意译"而非"音译"（`cigarette` → タバコ）
- 罗马音路径按 `di → ヂ` 切分，`diorama` 走罗马音会得到 ヂオラマ（走词典才是 ジオラマ）
